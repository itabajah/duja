# Refactor debt

Running list, drained by a dedicated `refactor:` PR at each phase checkpoint
(time-boxed to ~15% of the phase). Add entries during feature work instead of
detouring; delete entries when drained.

| Added | Where | What | Why deferred |
|---|---|---|---|
| P1 | P4 QA | Manually verify tray menu-click delivery (not scriptable; identical dispatch path as verified hotkey) | Needs human hand on shell tray |
| P2 | `.github/workflows/` | Add `coverage.yml` (llvm-cov ≥90% gate) and `fuzz.yml` (weekly nightly burn) CI jobs | Ran locally at the P2 gate; wire into CI in a P8 hardening pass |
| P2 | `duja-core` `sync`↔`config` | Wire `MonitorConfig.sync_offset` (now persisted) into the app's `SyncGroups` load/save path | Consumer lands in P4/P5 UI wiring; core plumbing complete |
| P3 | `duja-ddc` `win/sys.rs` | Validate `classify_failure`'s `GetLastError`-after-VCP-call assumption on real hardware; a gone monitor may classify `Timeout` instead of `Disconnected` | dxva2 VCP calls are not documented to `SetLastError`; needs a live unplug experiment (console session) |
| P6 | `duja-app` `--stress` harness | The stress report gates on **absolute zero** hardware errors ⇒ PASS. Live QA (2026-07-11) showed the real MSI MP273QP surfaces 1–2 transient DDC errors per ~300 inputs in ~1 run of 5–8 under 20–25 Hz flood (NAK/timeout past the retry budget) — handled gracefully (no cascade, no false-unresponsive, no panic). Change the gate to an **error-rate threshold** so a normal-flakiness run isn't reported as FAIL | Cosmetic harness policy, not a product defect; the runtime degradation is already correct. Revisit when the P8 soak numbers set a real threshold |
| P3 | `duja-app` `engine.rs` | First `SetUserLevel` before the initial-Get ack scales against the default max=100; wrong absolute level on non-100-max monitors until calibration lands (transient, self-correcting) | Needs a queue-behind-initial-Get design decision; harmless for P3 bring-up |
| P3 | `duja-app` `engine.rs` | Watchdog deadline re-stamps on every dispatch, so continuous slider input against a stuck worker defers detection until the user pauses | Semantics decision (per-op vs per-display deadline); revisit with P4 UI feel |
| P3 | `duja-app` `run.rs` | `PlatformEvent::Suspending` is dropped; no pre-suspend write quiescing | Consumer is the P4 dimmer/state re-apply work |
| P3 | `duja-panel` `wmi.rs` | `WmiMonitorID` array decoding, `WmiSetBrightness` invocation, and ProductCodeID assumptions never executed on real hardware (dev box has no internal panel) | Needs a 30-min borrowed-laptop run before P5 (plan §P3) |
| P4 | `duja-app` `engine.rs`/`run.rs` | Suspend/resume DDC re-push: on resume the display set is usually unchanged, so the manager emits no `Added`/`Reattached` and the engine never re-applies levels — a monitor that forgot its brightness across sleep (or a laptop panel reset by the firmware) stays wrong until the user nudges the slider | Needs hardware evidence (which monitors drop DDC state across S3/modern-standby) before choosing a policy: re-push all levels on `PlatformEvent::Resume`, or only after a resume-triggered enumeration diff |
| P5 / v0.1.0 | `duja-app` binary size | `duja.exe` is **~19 MB** vs the ≤16 MB ADR-0012 budget: the ureq/rustls/ring/webpki-roots update stack (+1.2 MB) **plus** the WinRT toast bindings the v0.1.0 smart update loop added (`UI_Notifications`/`Data_Xml_Dom`/`Foundation*`). Levers: fat LTO (−1.0 MB measured), feature-gate the update stack (network + toast) behind a default-on feature so a "lite" build drops both, drop `tracing-subscriber`'s `env-filter` regex | P8 hardening owns binary trimming; RAM and wakeup budgets still pass with headroom |
| P5 | `duja-core` `quirks` | User-directory quirk override (`quirks.override.toml`) is documented in the module + plan §7 but not wired — embedded DB only | Reduces attack surface today; wire with the P8 quirk-DB refresh from beta reports |
| ~~P5~~ | `duja-ui` | ~~Theme "Auto" resolves to dark: no OS dark-mode query is exposed by the pinned winit/slint~~ — **drained by the macOS OS-hook hoist**. The deferral reason was the stale half of the row: nothing ever required going through winit or Slint. (For accuracy: winit 0.30 *does* expose `Window::theme()`, but it needs a live window and Slint does not hand out the winit handle, so it is unreachable from where Duja resolves its palette — before either window exists.) `duja_platform::desktop::os_dark_theme` reads the OS directly on both platforms (`HKCU\…\Themes\Personalize\AppsUseLightTheme` on Windows, `AppleInterfaceStyle` on macOS) | Waiting on a Slint bump was waiting on the wrong thing; the query belongs in the platform crate either way |
| P5 | `duja-ui` settings | Sync-group management (create/assign/offset) has no UI, so `MonitorConfig.sync_offset` (persisted since P2) still has no consumer | Needs a group-management design, not a toggle; post-beta |
| P6 | `duja-ddc` `mac/` | macOS DDC/CI (enumeration + both I2C transports) has **never run on real hardware** — Duja has no Mac and CI mac runners are virtualized. Experimental until ≥3 independent community confirmations per architecture (Apple Silicon + Intel), per plan §P6 (ADR-0013). **The P6 gate showed what that costs.** "The pure packet codec is fully CI-verified" was the deferral reason in the next column and it was *true of the tests that existed* — which is exactly why it was worthless: the Apple Silicon request framing was malformed from the start (`#106`), the exact-byte corpus pinned the broken bytes as a regression baseline, and the fake bus shared the production misreading, so a fully green codec suite described a wire no display could answer. Purity buys host-testability, not correctness against an external protocol; only a **primary source** does that, and the reading has to be checked against more than one because they disagree | Hardware-blind by design, and the codec half is now cross-checked against four independent implementations (fastfetch, MonitorControl/m1ddc — one source, shared author — and `ddc-macos`, which dissents on the Get checksum) rather than against one. What is still unproven is everything `#106` could not reach: that a real display accepts these frames, that either I2C transport carries them, and that enumeration binds the right service. Those need a Mac |
| P6 | `duja-ddc` `mac/sys.rs` | Display↔I2C-service matching is **positional** (`CGGetActiveDisplayList` order — the row said `Online`, a different list; corrected at the P6 gate); two identical external monitors can mis-pair ("brightness on the wrong monitor"). **The remedy this row proposed is not the best one available.** It named MonitorControl's EDID-attribute scoring, which needs a multi-display Mac to tune. m1ddc instead resolves the service **per display, with no pairing at all**: `CoreDisplay_DisplayCreateInfoDictionary(id)["IODisplayLocation"]` gives an IOService-plane path, `IORegistryEntryCopyFromPath` walks to that node, and the `DCPAVServiceProxy` beneath it with `Location == "External"` is *that display's* service (`sources/ioregistry.m`). Duja already `dlopen`s that symbol and already reads that dictionary in `read_edid`. MonitorControl corroborates the signal's weight: it scores `kIODisplayLocationKey` at **10 of a 20-point maximum**, five times any EDID attribute. That dissolves this row rather than tuning it, and needs no hardware to write | Positional is correct for the common single-display case, so this is a correctness upgrade rather than a live defect. Prefer the `IODisplayLocation` resolution over the scoring port when it is picked up — it removes the class instead of trading accuracy for complexity, and it is the one remedy that does not need a Mac to *write* (it still needs one to *confirm*) |
| P6 (gate, `#109`) | `duja-app` `tray/state.rs` `on_platform_wake` | **The wake→re-assert wiring is unpinned at the app layer.** The coordinator half (`GammaCoordinator::invalidate`, four tests) and the engine half (`a_platform_event_announces_itself_before_any_enumeration_settles`) are both covered and both proven red. The three-line function that joins them is not: deleting `self.gamma.invalidate()` or `self.apply_overlays()` from `on_platform_wake` leaves the whole suite green. So the property "a resume re-asserts the ramp" is pinned at both ends and unpinned in the middle — and `#109` exists because an ADR precondition went unimplemented, which is the same shape | `AppState` cannot be constructed in a test: it owns two live Slint shells and a concrete `tray_icon::TrayIcon` whose only constructor does `CreateWindowExW` + `Shell_NotifyIconW` (the identical blocker recorded in the v0.1.1 throttle row, the `#82` row and the `#94` loop-assembly row). Mitigated the way those are — the gap is named here and in the function's own doc rather than left implied — and it closes with the same `AppState`-shaped refactor they are all waiting on, not with a test |
| P6 (gate, **a defect this row exists to prevent recurring**) | `duja-ddc` `mac/sys.rs` | **Do not "fix" the EDID-skip into a slot-consuming pairing.** The loop `continue`s on an unreadable EDID *before* taking a service, which reads like a queue desynchronisation; `#108` changed it to pair-then-filter and review blocked it. The two lists are filtered on different things and agree on exactly that population: `read_edid` returns `None` when `CoreDisplay_DisplayCreateInfoDictionary` yields no dictionary — the signature of a **virtual** display (Sidecar/AirPlay/`DisplayLink`) — and a virtual display has no `DCPAVServiceProxy`, so it never contributed a service to spend. Consuming one hands a **real** monitor's service to a display that cannot use it and then releases it: on an M-series Mac with a Sidecar iPad plus a Dell on USB-C, DDC control of the Dell disappears entirely. The reasoning is now in the `enumerate_displays` doc so the next reader does not re-derive the wrong conclusion | Fixed by not doing it. Kept as a row because the code *looks* wrong and the wrong fix is the obvious one — this is the second time a macOS assumption in this crate was "corrected" into a worse state (see the `#106` framing row). Two independent facts also rule out index-identity in general, both recorded at the call site: a hardware-mirror clone contributes a service without appearing in `CGGetActiveDisplayList`, and `CGDirectDisplay.h` makes the CG order user-reorderable (*"The first display returned in the list is the main display"*) |
| P6 (gate) | `duja-ddc` `mac/mod.rs` | **The two macOS tokens are written side by side as bare `u32`s, and a swap is undetectable.** `enumerate()` assigns `cg_display_id` (addresses one display) and `surface_id` (names a framebuffer, only ever compared) in one struct literal; transposing them compiles, lints clean on both Apple targets, and leaves the whole suite green — measured at the P6 gate. The module is `cfg(target_os = "macos")`, has no test module, and `enumerate()` returns empty on CI's virtualized runners, so nothing can reach it. Swapped, a `MacBook` mirroring to a projector drives gamma on the built-in panel — which this backend never enumerates — while the monitor being dragged does not move, and mirror grouping reverts to `#66` | `duja-panel` already solved the identical problem the right way: its assembly is the pure `panel_geometry(rect, mirror)`, tested on every lane, where the same swap reds (proven). Hoisting this one to match is a small, hardware-free change and is the natural next step; it was left out of the pairing fix to keep that PR's blast radius to one behaviour. The call site now carries the caution in prose meanwhile, which is the honest interim. The comment that used to sit there claimed `MirrorState` meant "there is no positional pair to get the wrong way round here" — that was a claim about this assignment, and it was false: `MirrorState` protects the FFI *read* site only. It is gone |
| P6 | `duja-ddc` `mac/sys.rs` | `io_return_to_result` maps every non-success `IOReturn` to `Timeout`; a truly gone display is only removed by re-enumeration, never surfaced as `Disconnected` from the transport. Distinguish `kIOReturnNoDevice`/`NotResponding` | Same live-unplug experiment gap as the Windows `classify_failure` debt; needs hardware |
| P6 → narrowed by `#104` (C6 packaging) | `duja-platform` `autostart/mac.rs` | Migrate macOS autostart from the `launchd` LaunchAgent plist to `SMAppService.loginItem` (the modern login-item API). ~~Blocked on a bundle existing at all~~ — **the bundle exists as of `#104`**: `xtask dist` produces `Duja.app` with the `CFBundleIdentifier` that `SMAppService` would register under (a test pins it byte-identical to this module's `LABEL`) | **The remaining blocker is the signature, not the bundle.** `SMAppService.loginItem` registers a *bundle identity*, and macOS resolves that identity through the code signature; an ad-hoc signature — which is what Duja ships until there is an Apple Developer account — has no stable designated requirement across rebuilds, so a login item registered by one build is not recognisably the same app as the next. The LaunchAgent plist has no such coupling: it names a *path*, which is exactly why it works today and why it keeps working for the portable/unbundled copy that will still exist after the migration. Also still true: macOS 13+ only, so the plist stays as the fallback either way. Revisit when `MACOS_SIGN` is turned on, not before |
| P6 (audit 2026-07-13) | `duja-panel` `Cargo.toml`/`wmi.rs` | Unify the `windows` crate version: panel is pinned to 0.58 while the rest of the workspace is on 0.62 (a cargo-deny `multiple-versions = "warn"` + a duplicate Win32 binding graph). The 0.58→0.62 VARIANT API changed (`as_raw`, `BSTR: TryFrom<VARIANT>` removed), so unifying needs a real WMI rewrite | Do it in the P8 pass that also runs the WMI set-path on real hardware (same borrowed-laptop gap as the `wmi.rs` row above) — changing untested COM bindings blind is the risk |
| P6 (audit 2026-07-13) | `duja-app` `tray.rs` / `duja-ui` `dpi.rs` | Decide whether the visible-window buffer re-assert (`enforce_logical_size` on the flyout re-enumeration path) is still needed now that Slint sizes windows natively (PR #29). If redundant, removing it makes the whole `enforce_*`/`size_to` show-path chain dead and deletable | Needs a live hot-plug-while-the-flyout-is-open test on the real monitor to confirm native sizing covers the row-count change; the current code works, so this is cleanup not a fix |
| P6 (audit 2026-07-13) | `fuzz/` | Add the `fuzz_config_toml` target (plan §4 names it) — `config.toml` is user-editable and parsed through chained `toml_edit` migrations, an untrusted-parse surface currently without fuzz coverage (caps/edid/quirks/ipc/ddc are covered) | Low marginal value until `fuzz.yml` runs targets in CI (also deferred, see the coverage/fuzz row above); add both together in the P8 hardening pass |
| ~~P6 (audit 2026-07-13)~~ | `.github/workflows/` | ~~Add `release.yml` (SHA256SUMS + build-provenance + minisign)~~ — **done in v0.1.0**. Still open: add `soak.yml` (nightly fake-backed soak) | `release.yml` shipped for v0.1.0; `soak.yml` folds into the P8 soak work |
| v0.1.0 | release signing | Release binaries are **unsigned** (no Authenticode cert) → SmartScreen warns on first run. Decide: buy an OV/EV cert, or an Azure Trusted Signing subscription, and add a signing step to `release.yml` | Authenticity is covered by SHA256SUMS + minisign + provenance meanwhile; a cert removes the SmartScreen friction that costs first-run installs |
| v0.1.0 | update channels | The checker uses GitHub `/releases/latest` (stable only), so a future `-beta` never prompts stable users. A real beta channel needs the `/releases` list endpoint + a channel setting | Fine for the stable line now; revisit when a public beta ships |
| ~~P6 (audit 2026-07-13)~~ | `duja-ui` / `duja-app` | ~~De-duplicate: `physical` (`dpi.rs`) ≈ `anchor_dim` (`positioning.rs`, named `physical_dim` until `#91`) are byte-identical logical→scaled-unit math across two crates~~ — **drained at the P6 refactor checkpoint**: both now delegate to `duja_core::scale::scale_extent` (`duja-core` is the only crate both depend on), with five tests including the `.max(1)` floor that a naive hoist drops — `logical.max(1.0)` alone is not enough, because a small honoured factor rounds a one-unit extent back to zero. **The two wrappers stay**, and that is the point: an anchor unit is physical pixels on Windows and points on macOS (ADR-0021) while `dpi`'s output is always physical pixels, so only the *arithmetic* is shared. Calling the core helper from the call sites would erase the distinction the ADR exists to keep explicit. ~~`clamp_pct` is duplicated in `shell.rs` and `settings_shell.rs`~~ — **also drained**: `settings_shell` now imports `shell::clamp_pct`. The first draft of this drain left it open on a claim that the two "are not the same function", citing the settings copy's note about the floor slider being "capped further to the view-model's max" — **review showed that was false**. The two were byte-identical in body *and* contract; that note is a cross-reference to `SettingsVm::set_monitor_floor` applying `MAX_FLOOR_PCT` **after** `clamp_pct` returns, and `clamp_pct` has no floor-slider awareness at all. An invented contract difference is a worse outcome than the duplication, so the row was finished instead | Both halves done. **Residue worth recording rather than draining blind**: the degenerate-scale guard (`is_finite() && >= 0.1 else 1.0`) still appears in four places — `duja_core::scale`, `positioning::flyout_height_cap`, `dpi`'s `Resized` arm, and `duja_platform::geometry::sane_scale`, which ADR-0021 §4 names as *the* canonical low-end guard both anchor factors route through. `duja-core` cannot depend on `duja-platform`, so unifying them is a real design question (which crate owns it), not a hoist |
| P6 (audit 2026-07-13) → narrowed in `#87`, again in the macOS OS-hook hoist | `duja-app` `toast.rs` | ~~Win32 cursor/work-area geometry~~ **hoisted to `duja-platform::geometry` in `#87`**; ~~`ShellExecuteW` (open the releases page) and `SystemParametersInfoW` (the reduced-motion query)~~ **hoisted to `duja-platform::desktop`** with the macOS assembly, as this row anticipated. One call site is left in the app binary: `SetCurrentProcessExplicitAppUserModelID` (`toast.rs`, the toast's app identity) | The rule that emptied the rest applies here too — FFI earns a normalized API once it has a second platform's implementation — and the toast has none. macOS user notifications need `UNUserNotificationCenter`, which requires a signed `.app` bundle *and* a runtime authorization prompt; `NSUserNotification`, the bundle-free predecessor, is removed. **`#104` supplied the bundle**, so the first clause is satisfied for an installed copy — but not for the bare binary a `cargo run` or a future portable build produces, and the authorization prompt is a product decision (a brightness tray asking for notification permission on first launch) rather than a plumbing one. So the macOS update path surfaces through the tray menu item and tooltip only, and a macOS toast is a post-packaging decision, not a hoist |
| ~~post-v0.1.5 (#87)~~ | `docs/adr/` | ~~No ADR records the tray-geometry coordinate contract~~ — **drained in `#91`**: [ADR-0021](adr/0021-tray-anchor-coordinate-contract.md) records it, and the open half it was waiting on is closed by `AnchorUnit` plus the two derived factors (`logical_to_anchor` × `anchor_to_physical` == the sanitised `scale`, on both variants) | The macOS backend that was supposed to close the question landed in the same PR, so the decision is whole rather than half. The part that still needs a Mac is *verification*, not design — see the mixed-DPI row below |
| post-v0.1.5 (#91) | `duja-app` `tray/state.rs` + `duja-ui` `shell.rs` | Mixed-DPI macOS: winit's `set_outer_position` divides the `PhysicalPosition` it is given by the scale factor of the screen the window is **currently** on, not the screen being targeted. `anchor_to_physical` multiplies by the *target* monitor's `backingScaleFactor`, so a flyout that moves between a Retina and a non-Retina screen can land off by the ratio of the two (e.g. 2× too far out when a window still parked on the built-in Retina display is positioned onto an external 1× monitor). A pre-show `set_position` on a never-yet-shown window is the likely-worst case, since its "current" screen is whatever winit picked | Needs real hardware to observe: which screen winit believes a hidden window is on, and whether the first `present_at` of a session even hits the mismatch, are not derivable from the docs and cannot be reproduced on Windows or in CI (macOS runners are virtualized single-display). Guessing a correction blind would be worse than the gap — it could break the single-display case that is the overwhelming majority. Fix once a mixed-DPI Mac can show the actual offset; the candidate is to position via a winit `LogicalPosition` (points) instead of pre-multiplying, which would make `anchor_to_physical` unnecessary on macOS |
| ~~post-v0.1.5 (#91 review)~~ | `duja-platform` `mac_geometry.rs` | ~~The cursor hit-test bias trades a top-edge defect for a bottom-edge one at 3×~~ — **drained in `#91` before merge, not deferred**: the row's own justification was unsound. It claimed narrowing `ε` "needs a Mac", but the derivation gives the safety condition as `0 < ε <= δ` (`δ = 1/backingScaleFactor`), so hardware could only ever reveal a *smaller* `δ` — which argues for a smaller `ε`, never a larger one. `0.5` was the **largest** admissible value; `ε = 0.25` satisfies the condition for every backing scale up to and including 4×, twice the density Apple ships. Verified by a brute-force sweep (7 layouts × steps 1.0/0.5/⅓/0.25 × every reachable reported row and column: `0.5` clean at 1×/2× and wrong at 3×/4×, `0.25` clean throughout) and pinned by `the_lowest_row_of_a_screen_still_belongs_to_that_screen` | Nothing was left to decide, so nothing was carried: the value is at a setting with 2× margin over shipping hardware, the remedy for a hypothetical denser display is to shrink the same one-line constant, and the condition is recorded on it. The sweep also surfaced a second, more common manifestation the row had missed — the probe falling outside **every** frame and taking the `Some(0)` primary fallback, which silently mis-routes a non-primary screen's lowest row even in a plain side-by-side layout — now covered by its own test |
| P6 (first-paint fix) | `duja-ui` `shell.rs`/`settings_shell.rs` + `.slint` | The `present-nonce` full-window repaint anchor is a workaround for winit 0.30 not emitting `WindowEvent::Occluded` on Windows (so the software renderer's cleared-surface cure never runs on re-show). Re-test at the next Slint/winit bump; if upstream emits `Occluded` (or the renderer force-refreshes on show), delete the property, the window-edge `background` binding, and the flip in both `present_at` | Upstream fix is the clean home; the anchor is a few imperceptible lines and fully covered by binding tests until then |
| P6 (external reflection) | `duja-platform` / `duja-app` `engine.rs` | External-change reflection is **poll-based and visible-only**: it runs only while the flyout is open (to keep the idle engine at zero wakeups) and reads over DDC. A Windows `WmiMonitorBrightnessEvent` sink would reflect **internal-panel** changes (laptop brightness keys) instantly, even while the flyout is closed, without polling. Add it as a `PlatformEvent::BrightnessChanged` source feeding the same `LevelRead` path | Polling covers external DDC monitors; the dev box has no internal panel (same borrowed-laptop gap as the `wmi.rs` rows), and the event sink is the richer long-term source |
| P6 (review 2026-07-13) | `duja-app` `tray.rs` `adopt_position` | The pre-probe placeholder `DEFAULT_USER_LEVEL_PCT = 50` is **in-band**: a genuine hardware reading of exactly 50 % is indistinguishable from "not yet learned", so on launch a panel sitting at exactly 50 % with a divergent persisted level adopts the persisted value instead of `reverse_map(50)`, causing a first-touch jump. Fix needs an out-of-band "learned" signal in the snapshot (e.g. `Option<u8>`), which is a `DisplaySnapshot` API change | Narrow trigger (hardware *exactly* 50 % + divergent persisted); the invasive snapshot change isn't worth rushing during the continuum-v2 landing |
| P6 (review 2026-07-13) | `duja-app` `tray.rs` `on_level_read` | When an external agent drives the hardware **below the configured floor** on a *user-controlled* display, the reflection lands the slider below the transition and `apply_overlays` re-plans a sub-floor overlay computed as if hardware were pinned at the floor — but reflection never re-writes hardware, so the overlay double-dims over the already-lower hardware (one-shot, persists until the next user action). Fix: a reflected below-floor change should plan alpha 0 (the external agent owns the hardware) rather than the pinned-floor overlay | Narrow (floor > 0 **and** an external agent going below it); the correct fix is a small design decision about reflection↔overlay interaction, best done deliberately |
| P6 (review 2026-07-13) | `duja-ui` `flyout.slint` | The premium slider's value bubble (`y:-28px`) and marker hover-label chips (`y:root.height-2px`) render outside the 28 px slider band and, with no `clip`, overlap the card's header row / dimming-toggle row while dragging/hovering. Give them a reserved clip-free band or reposition | Cosmetic, transient (drag/hover only); best tuned against a live render during the flyout visual-QA pass |
| P6 (review 2026-07-13) | `duja-app` `engine.rs` | A **failed** initial-learn `Get` (`Err`) never clears `pending_learn` (the clear sits inside the `Ok` arm), so `poll_levels` skips that display indefinitely — level polling is silently disabled for it until a user `SetUserLevel` or a retire. Pre-existing (the `Ok`-gated clear predates polling); polling just made the consequence observable | Rare (an `Err` from a *responsive* display; a hang is caught by the watchdog instead); clear `pending_learn` on `Err` too when the learn path is next revisited |
| P6 (UI polish 2026-07-14) | `duja-ui` `settings_shell.rs` / `duja-app` `tray.rs` | The user-resized settings-window size is **not persisted**: it resets to the 560×700 preferred size on every open. Persist it to `config.toml` (a `[ui] settings_size` field) and seed `present_at`/`preferred-*` from it | Ships in-session resize first; persistence is a small, orthogonal config follow-up not needed to satisfy "resizable" |
| P6 (UI polish 2026-07-14) | `duja-ui` `settings.slint` / `settings_shell.rs` / `dpi.rs` | Frameless interactive resize (custom edge/corner grips → winit `drag_resize_window`, `preferred-width`/`min-width` sizing, and the `Resized`→`desired` capture) is verified to **compile and instantiate** headless but its live behaviour with the software renderer (smooth repaint during drag, min-size enforcement, no partial frames) can only be confirmed on a real display. Re-test at the next Slint/winit bump — if a cleaner resizable-frameless path appears upstream, simplify | GUI resize is not headless-testable; the implementation follows the documented winit API and the existing `drag_window` move pattern |
| P6 (UI polish 2026-07-14) | `duja-ui` `settings.slint` | The right-edge (E) resize grip covers the outer 6px where the settings `ScrollView`'s vertical scrollbar is drawn, so dragging the scrollbar thumb's outermost ~2px starts a resize instead of a scroll (only when the content actually scrolls; wheel + most of the thumb are unaffected). Reserve a scrollbar gutter or inset the E/right-corner grips off the scrollbar lane | Marginal edge-vs-scrollbar conflict inherent to a frameless right-edge grip; not worth distorting the layout until live QA says it bites |
| P6 (UI review 2026-07-14) | `duja-ui` `flyout.slint` | The per-row "Software dimming" caption lives in a fixed `54px` column at 9px so it wraps to two lines in English; a long non-breaking translation (e.g. German "Softwaredimmung") cannot wrap in 54px and would overrun/clip the column. Widen or `elide` the caption, or shorten the localized key, when translations land | Latent i18n-only — the shipping English string fits cleanly; a blind fixed-width/elide change can't be visually verified headless, so it waits for a locale pass |
| P6 (accent themes 2026-07-14) | `duja-ui` `flyout.slint` | The slider's marker lines (`Palette.marker`, lines A and B) are drawn *over* the accent fill, so where the fill covers them they can go low-contrast under a light accent (Onyx-dark: 1.1:1). Mitigated by geometry — the markers are `track-h + 6/8 px` tall, so ~4px nubs protrude above/below the fill onto `surface` and stay legible; and they only render when a hardware floor is configured. Fix, if live QA rejects it: add a `marker-on-accent` family member | Cosmetic and partially self-mitigating. It must be a *hand-tuned* table cell rather than an automatic luminance rule — an automatic rule would flip ruby's markers, changing the look that shipped in #39 |
| P6 (accent themes 2026-07-14) | `duja-ui` `flyout.slint` / `settings.slint` | The focus ring on a **checked** `PillToggle` / a **primary** `MiniButton` is drawn on top of the accent fill and is near-invisible there — ruby already scores 1.20:1 today. A one-line fix exists (`checked ? Palette.accent-on : Palette.focus-ring`) | **Pre-existing, not introduced by the accent work** (no accent is worse than ruby is today). The fix is a strict improvement but *visibly changes ruby's* focus ring from coral to white, so it needs an explicit design OK rather than being smuggled in |
| v0.1.1 (deep review) → narrowed in `#82` | `duja-app` `tray/state.rs` | The **throttle-final-value** contract (P4 gate Finding 1: a leading-edge UI throttle drops the *final* sample of a slider drag, stranding the hardware mid-drag) is pinned at the `duja-ui` end (`shell.rs`'s `slider_drag_burst_emits_the_released_value_last`, which drives the real Slint binding through `FlyoutVm::slider_changed` + `FlyoutShell::on_command`) and at the engine end (`write_min_gap`, last-wins). **The app layer between them is still unpinned**: `AppState::on_ui_command`'s `SetLevel` arm and `AppState::set_user_level` — the historical defect site, which guards the `LevelForwarder::forward` call. A throttle re-added at either one passes the entire suite; verified empirically, not assumed. `LevelForwarder`'s own tests do **not** cover this — the forwarder is downstream of both | `AppState` cannot be constructed in a test: it owns two live Slint shells (`FlyoutShell`/`SettingsShell`) and a real `tray_icon::TrayIcon`, a concrete non-fakeable handle whose only constructor does `CreateWindowExW` + `Shell_NotifyIconW`. Closing the gap needs those fields behind traits/`Option` (rippling through `tray.rs`/`wiring.rs`/`state.rs`) or a tray-thread harness — a refactor of its own, deliberately not smuggled into a test-infra PR. Mitigated by explicit "this is NOT covered" notes at both ends (`set_user_level`, `on_ui_command`, `level_forward`'s module doc) so the next maintainer is not misled |
| v0.1.1 (`#53`) | `duja-app` `tray.rs` | HDR gamma verdict is now **live** (re-probed per enumeration/interaction) but still a single **global** OR of `is_hdr_active`, not per-display. A mixed HDR/SDR multi-monitor setup applies the strictest verdict to all | Per-display HDR needs the verdict threaded through the plan per display; the global live verdict already fixes the frozen-at-launch brick. Future refinement |
| v0.1.1 (`#55`) | `packaging/windows/duja.iss` + `duja-app` | Installer now **detects** a running instance (`AppMutex`), but seamless **auto-close** via the Restart Manager (`CloseApplications` + WM_QUERYENDSESSION handling in the winit pump) is not wired — an in-place upgrade still asks the user to close Duja | Detection is the high-value, low-risk half; auto-close needs message-pump work on the Slint/winit loop. Follow-up |
| ~~v0.1.1 (`#54`)~~ | `duja-app` `worker.rs`/`engine.rs` | ~~Honor `ddc_broken` in the tray app~~ — **delivered in v0.1.2 (#59)**: the worker now probes on open, so a `ddc_broken` monitor (empty caps ⇒ `hardware_range: false`) is downgraded to `SoftwareOnly` full-range software dimming instead of getting dead VCP 0x10 writes | Was a documented-deferred static-caps design; the no-hardware detection wave now covers it (probe-on-open + verify-first-write) |
| v0.1.1 (`#52`) | `duja-ui` `settings_vm.rs` / `flyout.slint` | The Input dropdown's recorded selection resets to `None` on the next `set_displays` (hot-plug / dimming toggle / settings reopen), so it sticks only for the immediate interaction; and the `.slint` `current-index` binding itself isn't unit-pinned (only the VM recording is). Also: a concurrent **unplug of a different display** during a slider drag can still shift the dragged row (order-stable handles additions; removals compact) | Empty-until-picked is honest (no active-input readback exists); persisting it needs an input-readback in the snapshot. The drag-vs-removal edge is rare and the reflection path re-settles the level. Visual-QA the dropdown-empty + scrollbar-inset |
| v0.1.1 (`#48`) | `duja-app` `worker.rs` / `protocol.rs` | A `controller.set()` that returns `Ok(Err(ControlError))` (a handled backend rejection, not a panic) is acked as a **successful** Set, so a monitor that persistently rejects writes is never surfaced/greyed | Intentional per the protocol doc (a gone display is caught by the next enumeration's `Removed`); surfacing persistent write-rejection needs a new transient-vs-persistent policy + `AckOutcome` variant. Revisit if a real monitor exhibits it |
| v0.1.1 (`#49`) | `duja-dimmer` `win/mod.rs` | `WindowsDimmer::shutdown()`'s `join()` is still **unbounded** if the overlay worker is wedged in a hung Win32 call — the dispatch `recv_timeout` unblocks apply/clear (and quit reaches `quit_event_loop`), but a Drop-time join could stall final teardown after the UI is gone | Stable `std` has no timed join; a real fix needs detach-or-timeout semantics like the engine's. The primary UI-freeze is fixed; this is teardown-only |
| ~~v0.1.1 (deep review)~~ | `duja-platform` `ipc/unix_socket.rs` + `single_instance.rs` | ~~**Live on macOS since `#102`; Linux from P7.** The unix stale-socket takeover is a non-atomic unlink+rebind (two concurrent starts → two live servers), and the `/tmp/duja-<uid>` fallback dir is squattable by another local user (`ensure_dir_0700` trusts a pre-existing dir; fails OPEN). Fix with `create_new` + fstat owner/mode + `O_NOFOLLOW`, or require `XDG_RUNTIME_DIR`~~ — **fixed in `#114`**, both halves, in the order this row prescribed. **The whole bind** runs under a sibling `flock`, not just the takeover: `UnixListener::bind` is `socket`/`bind`/`listen`, and an instance parked between the last two is indistinguishable from a stale inode to a concurrent probe, so locking only the takeover left the race narrowed rather than closed. Shutdown's unlink is conditioned on the same lock plus a `(dev, ino)` check, because a departing server would otherwise delete its *successor's* socket — the same "two servers, one unreachable" symptom reached with no takeover involved at all. **An inode number alone was not enough for that check, and the place it fails is the one this row already identifies as the Linux exposure.** ext4 and XFS recycle inode numbers, so once the departing server's fd is closed and the successor unlinks the old inode, the final `iput` reaches `ext4_free_inode`, the number returns to the block group's inode bitmap, and `ext4_new_inode` allocates the lowest free bit in it — which **can** be the number just recorded, making the comparison match on a coincidence. (Not *likely*: that bitmap spans a whole block group, typically 8192 inodes, not one directory. The guard has to be exact regardless.) tmpfs allocates from a monotonic counter (`get_next_ino`) and APFS assigns object ids monotonically, so `$XDG_RUNTIME_DIR` and macOS are immune; the `/tmp/duja-<uid>` fallback, the "routine cron/ssh/container condition" below, is usually ext4. The server therefore holds an **`O_PATH` descriptor** on the socket's inode until after the unlink: `ext4_free_inode` runs only from `ext4_evict_inode`, so a referenced inode cannot be freed and the successor's rebind is guaranteed a different number. The first attempt at this pinned the inode by dup'ing the **listening socket** instead, which works but keeps the socket *connectable* - and review round 3 showed that cost more than it bought. If both handler threads die the listener exits while the socket stays in `LISTEN`, so `dujactl` connects, blocks for the read timeout and exits with a server error, where before it got `ECONNREFUSED` and **fell back to driving the hardware directly**; and during shutdown a concurrent start is told "already listening", which `duja-app`'s IPC bridge does not retry. `O_PATH` references the inode and nothing else. It is Linux-only, which is where the problem is. The lock file is deliberately never unlinked: `flock` locks an inode, so unlink-and-recreate would let two processes lock two inodes and each believe it was exclusive. The directory check went to a shared `unix_dir` module (`O_NOFOLLOW | O_DIRECTORY` + fstat on the descriptor) used by **both** subsystems, which is what the row meant by "together". **Two corrections to this row.** (1) Only `single_instance` failed *open*. The socket half failed **closed**: `prepare_socket_dir` chmod'd the directory, which is `EPERM` on another uid's, so a squat stopped the server rather than being adopted by it. Its real hole was different and this row never named it — `set_mode` follows **symlinks**, so a symlink planted at the path redirected both the chmod and the bind. (2) `create_new` + fstat is the right shape, but refuse-on-mode is the wrong cut. The line is **writability**, not looseness. Write permission on a directory is permission to create, rename and unlink entries in it, so a group- or world-writable one may *already* hold an attacker's `ctl.sock` (and `PipeClient` performs no server-identity check, so `dujactl` would act on forged replies) or a lock file they hold a `flock` on; a late `chmod` does not undo any of it, so it is refused. Merely readable is repaired, because it grants only traverse and list against `0600` files — and it is what `create_dir_all` leaves under an ordinary umask, i.e. the state a caller that makes the directory first actually produces. Both wrong cuts were made before the right one: refusing every loose mode broke `stale_socket_is_taken_over`, and repairing every loose mode was unsound. Residual, a limit rather than an omission: the **foreign-owner** refusal is the one arm CI cannot execute, since it runs as a single user, so it is covered the way `peer_allowed` is — the pure decision unit-tested over a chosen grid of uid/mode pairs, the `stat` feeding it unverified. The concurrency test is likewise one-sided (a green run cannot prove a race absent), which is why the deterministic half is an assertion that the bind lock file exists at all | ~~Both are `#[cfg(unix)]` and unreachable on the shipping Windows build; fix before the P7 Linux port, together~~ — **the deferral reason is now false, re-triaged at the P6 gate.** `ipc/mod.rs` selects the unix socket on `cfg(unix)`, and `ipc/unix_socket.rs`'s own header now reads "macOS now, Linux in P7": the code is unchanged but it is **live on every macOS build**, so neither issue is hypothetical any more. `SECURITY.md` advertises "0600 unix socket in a 0700 dir" with no caveat. This must be fixed **before a macOS artifact ships**, not before P7 — and the release is held, so there is time. **The two halves are not equally live, and the order matters for whoever fixes them.** The **non-atomic takeover** (`unix_socket.rs`'s probe → `remove_file` → `bind`) is path-independent and therefore fully live on macOS: fix it first. The **squattable `/tmp`** half is *less* reachable on macOS than on Linux, not more — `socket_path` reads `XDG_RUNTIME_DIR` only under `cfg(not(target_os = "macos"))`; the macOS arm resolves `~/Library/Application Support/duja` and falls back to `/tmp/duja-<uid>` only when `HOME` is unset, which launchd and login always set. `single_instance.rs` goes further and uses `ProjectDirs::data_dir()` on macOS, degrading to "first" rather than touching `/tmp` at all — so `ensure_dir_0700` is not reachable via `/tmp` there. On Linux an unset `XDG_RUNTIME_DIR` is a routine cron/ssh/container condition, which is where that half actually bites. **All of this is now history — `#114` closed both halves as P7 wave 0, before any Linux code was written.** The re-triage above is what made that the first PR of the phase rather than a footnote inside it |
| v0.1.2 (`#59`) | `duja-app` `tray.rs`/`engine.rs` | A display *wrongly* downgraded to `SoftwareOnly` by the first-write verify is transiently double-dimmed (the residual hardware write to `user_pct` + overlay on top ⇒ visibly too dark) until the poll self-heal restores it on the next flyout-open poll | Now *bounded* by the self-heal (was permanent pre-review) and wrong-force is rare after the 3-attempt retried verify; a tray-side "don't write hardware for a `SoftwareOnly` display" guard would remove the residual entirely — small deliberate follow-up |
| v0.1.2 (`#59`) | `duja-app` `engine.rs` | The self-heal's `moved` guard could be tricked by a read-*jittering* dead panel that sits within ±1 of the user's written level (false-heal ⇒ the dead slider returns until replug, since a healed worker stays `settled`) | Very narrow (a dead panel whose reads jitter into the written band); the movement requirement makes it improbable and a replug re-detects. Note only |
| v0.1.2 (`#61`) | `duja-ddc` `win/mod.rs` | Mirror mode: if a genuine external fails all 3 *paced* probe reads (now rare) the external falls back to positional handle pairing and its flaky handle may be dropped (backstopped by the #59 verify→`SoftwareOnly` downgrade); and two *distinct* externals mirrored on one source both answer DDC, so the probe cannot disambiguate them | Fully solving needs EDID-over-DDC handle identification (read each handle's EDID and match the CCD target) — a larger FFI effort; the paced-retry + #59 backstop makes the residual rare and non-catastrophic. Validate on real dual-external mirror hardware |
| ~~v0.1.2 (`#58`)~~ | `duja-app` `backend.rs` | ~~The DDC↔WMI dedup only catches *serial-bearing* duplicates; a *serial-less* internal panel derives different ids across backends (`from_edid` vs `from_parts`) and would not dedup~~ — **reworked in v0.1.3 (#64)**: the merge no longer id-matches at all; a DDC-fallback internal panel is deduped by the *WMI-has-any-panel* signal (which also covers the serial-less divergence), and an internal panel WMI cannot see is now *surfaced* via DDC rather than skipped | Superseded — the id-hash divergence no longer blocks dedup. The remaining residual is the dual-internal-panel case (v0.1.3 row below) |
| v0.1.2 (audit) | `duja-app` `backend.rs`/`dimming.rs` | A **WMI** internal panel whose brightness writes silently no-op is downgraded to `SoftwareOnly`, but WMI panels carry `None` bounds, so `dimming::plan` emits no overlay: the "Software" slider then controls nothing. **v0.1.3 (#64) resolves this for a DDC-fallback internal panel** — it now carries real bounds and *is* overlay-dimmable; only a pure-WMI panel still lacks a bounds source — and `#105` settled the macOS side the same way, by giving a `DisplayServices` panel real bounds through `duja-panel`'s API, so what is left here is Windows-only and is a genuine WMI limitation rather than unwired plumbing | Give a pure-WMI internal panel a bounds source (its monitor rect via the device path), or suppress the software-only downgrade for a bounds-less `InternalPanel`, in the P6/P8 pass |
| v0.1.2 (audit) | `duja-ui` `flyout.slint`/`flyout_vm.rs` | When the *dragged* linked slider has a non-zero offset and the group saturates at a bound, the committed value is the clamped fan-out target while the Slint thumb follows the pointer, so on release the thumb snaps to the committed value | Cosmetic flip-side of the (correct) margin-preservation; the during-drag thumb just doesn't reflect the clamped value. Tune against a live render in a UI-polish pass |
| v0.1.3 (`#64`) | `duja-app` `backend.rs` | The discovery merge drops **all** internal-DDC entries when WMI lists *any* panel (the coarse WMI-presence dedup that also handles serial-less id divergence). On an exotic dual-internal-panel laptop (ASUS Zenbook Duo, Lenovo Yoga Book 9i) where WMI sees one panel but not a GPU-driven second, that second internal panel is dropped and vanishes — the same failure this fix cures, in a rarer shape | ≤1 internal panel is the universal case; a precise per-device dedup needs the PnP device-instance path (shared by the WMI `InstanceName` and the DDC monitor device path) plumbed through both backends. Exotic hardware, not the reported laptop |
| v0.1.3 (`#64`) | `duja-app` `backend.rs` (pre-existing) | `discover_panel` collapses `Err(PanelError) ⇒ Vec::new()`, so a **transient** WMI/COM fault is indistinguishable from genuine empty-WMI. On a WMI-capable laptop that faults for one enumeration, the internal panel is surfaced via DDC with a `from_edid` id; when WMI recovers, the id flips to `from_parts` (serial-less), which the engine sees as remove+add | Pre-existing collapse; the new fallback degrades to a divergent-id entry rather than nothing (strictly better than the old vanish). Distinguish a transient COM/WMI fault from real absence in the borrowed-laptop WMI hardening (same gap as the `wmi.rs` rows) |
| v0.1.3 (`#66`) | `duja-ui` settings / `duja-app` `tray.rs` | A mirrored set is merged into ONE flyout control keyed on the anchor's per-monitor config, but the settings window still lists every **physical** panel — so editing a NON-anchor member's floor / dim-mode is **inert** while mirrored (the group applies only the anchor's config). No wrong dimming, just a settings row that does nothing until the mirror breaks | The settings window stays per-panel by design (it must survive Duplicate↔Extend and drive each panel's own persisted config); hiding or annotating non-anchor rows *while mirrored* is a UX follow-up, not a correctness fix. Revisit with the settings per-monitor rework |
| v0.1.5 (software-only fix) | `duja-ddc` `controller.rs` | `probe_by_reads` no longer reports a **failed** brightness read as `hardware_range: false` (that misfire, on a fresh worker probing an asleep/DPMS/busy monitor, permanently mislabeled a working external monitor "software-only" until restart — the reported bug). The narrow flip-side: a DDC panel that answers *nothing* at all (no caps read, no VCP read, and whose writes also only ever `Timeout`) is no longer auto-flagged software-only at probe time — it stays hardware-backed with an inert slider until the user turns on software dimming via the flyout pill | The genuine no-hardware verdict now comes only from a *definitive* signal (a successful caps read lacking `0x10`, the `ddc_broken` quirk, or the retried first-write silent-no-op) — the intended detector per `backend.rs`. A fully-silent DDC panel is a rare edge (internal panels are WMI-first) and has a user-accessible workaround (the pill), whereas the false-positive it replaces needed a full restart. A transient-vs-persistent write-failure downgrade would close the edge; deferred as it risks re-introducing the slow-monitor false-positive |
| ~~post-v0.1.5 (tray split, #81)~~ | `duja-app` `tray.rs` | ~~Three doc-comment attachment slips spotted during the module split~~ — **drained in the same PR after review**: the `ReentrantCell` explainer (the one describing the `0xe06d7363`→`0xc0000409` crash cure) now attaches to the struct instead of the intervening `type Deferred<T>`; `build_flyout`/`build_settings_window` got their opening lines back; and `with_app_ref` — which the `APP` doc already claimed existed — is now a real function, which also let `APP` drop out of `wiring.rs`'s imports so the thread-local is reachable from `tray.rs` alone | Fixed rather than deferred: the review pointed out that adding `with_app_ref` *hardens* the re-entrancy invariant (a submodule can no longer name `APP` and take a raw borrow), so it was worth doing immediately rather than carrying |
| ~~post-v0.1.5 (tray split review, #81)~~ | `duja-dimmer` `tests/windows_live.rs` | ~~`drop_shuts_down_and_removes_overlays` failed once (`left: 2, right: 1`); these tests race the window server~~ — **drained 2026-07-30, and the recorded cause was wrong.** Not a window-server race: two independent defects in the 20-line enumeration helper, both now fixed. (1) The walk used `FindWindowExW`, whose `hwndChildAfter` Microsoft documents as resuming "with the next child window in the **Z order**" — a Z-order-relative cursor over a class matched across *every* process. A concurrent overlay elsewhere (a real `duja.exe` while it is software-dimming) could therefore make the walk return one window **twice** (⇒ exactly the recorded `left: 2, right: 1`) *or* **truncate early** when the handle held as the cursor was destroyed mid-walk (⇒ an undercount, `left: 2, right: 3`). Both reproduced on demand by running 2-3 copies of the test binary concurrently. Replaced with an `EnumWindows` snapshot, which is what Microsoft recommends over a `GetWindow`/`FindWindowEx` loop for these two reasons, and which has no cursor to invalidate — so the dedup, a cycle guard and the truncation all cease to be needed. (2) Attribution is only ever per *process*, never per *dimmer*, so sibling tests miscounted each other under a shared-process harness: `cargo test -p duja-dimmer` failed most of its tests nondeterministically (counts varying with `--test-threads`; `alpha_change_round_trips` read a sibling's window, `left: 128, right: 64`). Fixed with a 9-line `static Mutex` gate taken first in each test (released after the dimmer drops, so overlays are gone before the lock frees). `cargo test` now passes 7/7 across repeated runs; nextest unchanged at 49 | Both halves fixed rather than documented around, because `CONTRIBUTING.md` lists `cargo test --workspace` as the *first* developer workflow — so the red path was the documented one, and no doc change was needed once the tests were right. The gate costs nothing under nextest (one process per test ⇒ uncontended). A pre-spawn baseline diff was implemented first, measured as still-failing (a sibling's window can appear after the snapshot) and reverted rather than left in place implying a robustness it did not deliver. Honest limit: the *specific* historical run was never re-obtained, so (1) is the mechanism the code permitted plus a reproduction of that mechanism, not a post-mortem of that run |
| ~~post-v0.1.5 (#86)~~ | `duja-app` `motion.rs` | ~~`os_animations_enabled` returns a flat `true` on non-Windows, so once the flyout ships on macOS it overrides a user who explicitly asked the system to reduce motion — an accessibility regression~~ — **drained by the macOS OS-hook hoist**. `duja_platform::desktop::animations_enabled` answers it with `!NSWorkspace::sharedWorkspace().accessibilityDisplayShouldReduceMotion()`, and `motion.rs` keeps only the pure glide policy | The deferral reason — "reaching `NSWorkspace` means adding `AppKit` to `duja-app`, which belongs to the macOS app assembly rather than being smuggled in ahead of it" — was sound and was dissolved rather than overruled: the query went into `duja-platform`, which already had `AppKit`, so `duja-app` gained no dependency at all. The sibling `os_dark_theme` row above went the same way for the same reason |
| ~~post-v0.1.5 (#86)~~ | `duja-app` `tray.rs` `os_dark_theme` | ~~Returns `None` unconditionally, so the UI's "Auto" theme resolves to dark everywhere~~ — **drained by the macOS OS-hook hoist**, and not the way this row predicted. It assumed the fix had to wait for the tray to be un-gated, because `os_dark_theme` lived *inside* the `cfg(windows)` tray module. Moving the query into `duja_platform::desktop` removed that coupling entirely, so both halves landed before the un-gating rather than after it. The macOS side also does **not** use `NSApp.effectiveAppearance` as this row proposed: that needs a live `NSApplication`, and the call happens during startup before Slint's winit backend builds one, so touching `sharedApplication` there would create the app object out from under winit. `NSUserDefaults`' `AppleInterfaceStyle` answers the same question with no such requirement | Nothing carried |
| ~~post-v0.1.5 (#86)~~ | `duja-app` `backend.rs` | ~~The three `#[cfg(not(windows))]` hardware stubs — `discover_ddc` → `Vec::new()`, `open_ddc` → `None`, `open_panel_controller` → `None`~~ — **drained in `#90`**: `duja-app` and `dujactl` now open real macOS displays. `discover_ddc` got a macOS arm (kind always `ExternalDdc` — the mac backend filters built-ins with `CGDisplayIsBuiltin`, so there is no internal-DDC fallback carrier — and the display-surface token is the decimal `CGDirectDisplayID`); `open_ddc` and `open_panel_controller` became single `#[cfg(any(windows, target_os = "macos"))]` definitions (the Windows bodies compiled verbatim, as predicted), with the stubs narrowed to `not(any(windows, target_os = "macos"))`. So the `--once` inaccuracy is gone: a `hardware_range: true` panel row on macOS now has an opener behind it. `DisplayGeom` is re-documented (element 2's unit is physical pixels on Windows, **points** on macOS; element 3 is an opaque platform surface token with two invariants and two consumers), and `merge_displays`/`open_controller` say "the panel backend" instead of "WMI" | Was never dependency-blocked, only unwired; the residuals (never-executed-on-hardware paths, no macOS gamma sink, no shared-surface identity in the macOS token) are the row below |
| post-v0.1.5 (#90) | `duja-app` / `dujactl` `backend.rs` (macOS) | Three residuals from wiring the macOS backends. **(1) First execution ever.** No macOS DDC or `DisplayServices` code has run against real hardware — CI's mac runners are virtualized and expose no external DDC display, so `enumerate` returns empty there and every opener is exercised only by its types. Running `duja --once` or `dujactl list`/`get`/`set`/`doctor` on a real Mac is therefore the **first** execution of `DdcDisplay::into_controller`, the two I2C buses, `PanelDisplay::open`→`DisplayServicesGetBrightness`/`SetBrightness`, and — since `#105` — the `CGDisplayBounds`/`CGDisplayMirrorsDisplay` reads that place the built-in panel. Per ADR-0013 the DDC path stays labelled **experimental** until ≥3 independent community confirmations per architecture. ~~**(2) No macOS gamma sink.**~~ **Drained by the macOS gamma sink.** `gamma.rs` now carries a `MacSink` alongside `GuardSink`, driving `set_gamma`/`restore_identity` off the `gamma_token`. Two residuals moved to their own rows below rather than being carried here: the reports-success-without-applying hazard (which turns out to be a macOS problem too, not a Windows one) and the undocumented restore-on-exit belief the no-marker design rests on. ~~**(3) The mirror merge is inert on macOS, and wiring it naively would re-introduce `#66`.**~~ **Drained by the surface-token split.** The rule it recorded — never feed `device_for` into `clone_group` on macOS — is discharged, though not the way this row proposed: stamping the mirror-set master fixes grouping but **cannot** double as the gamma address, because a clone's token names another display and, on a `MacBook` mirroring to a projector, that display is the built-in panel `enumerate` filters out. `DisplayGeom` therefore carries two named tokens rather than one opaque string. What is left is a *new*, narrower row below (the hardware-blind premise, plus one-`DimCommand`-per-surface gamma coverage), not a carry-over of this one. | (1) is hardware-blind by construction (same gap as the `duja-ddc mac/` rows) and cannot be closed by any code change — it needs a Mac, and the wiring is the prerequisite for someone with one to test at all. (2) is drained, and the deferral reason turned out to be wrong in the useful direction: it predicted the sink would need "a `ScreenStateGuard` twin and a crash-marker policy — a design decision, not a port". It needed neither. (3) is drained; it was deferred because `CGDisplayMirrorsDisplay` semantics cannot be confirmed without a Mac, which is still true — what changed is that the fix no longer has to be *trusted* to be safe, because addressing was moved off the token entirely. |
| ~~post-v0.1.5 (#90)~~ | `duja-app` `backend.rs` / `duja-panel` (macOS) | ~~A macOS built-in panel enters the app as `(id, None, None)` — the same shape as a Windows WMI panel — so `dimming::plan` emits **no** `DimCommand` for it and it cannot be software-dimmed at all~~ — **fixed in `#105`**, by the route this row asked for. `duja-panel` gained a public `PanelGeometry` (bounds + the two dimming tokens) reported at `enumerate` time, `Some` on macOS and `None` on Windows, so the app folds it in without ever interpreting `instance_name` and without a line of display FFI. Three things had to move together, and the second is the one a narrower fix would have got wrong. **(1) Bounds** — `CGDisplayBounds`, in points, so the planner emits a `DimCommand` and the overlay covers the panel. **(2) The surface token** — bounds *alone* would have been a regression, not a fix: a `MacBook` mirroring its screen to a projector would then have had the panel (a `None`-token singleton) and the monitor at identical bounds in two groups, i.e. two overlays stacked on one framebuffer, which is `#66` reached from the other side. **(3) The gamma token** — the panel's own id, so a gamma-mode panel is addressed as itself rather than left with `overlay_alpha == 0` and nothing driving it. The surface rule and the `CGRect`→`DisplayBounds` conversion moved to a new pure `duja_core::macos` — not for tidiness: `duja-ddc` computes the external clone's token and `duja-panel` the master's, the app compares the two strings, and two copies of a rule that must agree exactly is the defect that arrangement invites. Both are FFI-free and tested on every lane | Residuals, all of them hardware-blind rather than unfinished. **(a)** The two new CoreGraphics reads have never run — the same gap as every other macOS row here, and CI's runners cannot close it. **(b)** The mirror case is where being wrong would show, and it is worth being precise about *how*. A macOS panel could previously never be merged away; now it joins a group, and — because `build_group` anchors on the lowest id string and an Apple panel decodes to `APP-…` (or the `AAP-…` sentinel), which sorts ahead of DEL/GSM/LEN/MSI/SAM/VSC and nearly every other PNP id — it will usually be the **anchor**. `plan_commands` takes the group's bounds from the anchor, so in a `MacBook`-to-projector mirror the merged set's single overlay is now placed from the *panel's* `CGDisplayBounds`, read from `CGGetOnlineDisplayList` (which `duja-ddc` never consults). If a hardware mirror reports a built-in rect that is not the shared mirror rect, the one overlay covers the wrong region on both screens. Non-anchor is the other half and is milder: `#66`'s open residual leaves that member's settings row inert. Both need the same Mac as the macOS-surface-token row below, whose premise this now shares and widens. Partly bounded already: a rect that is not finite or encloses no area — `CGRectNull`, what CoreGraphics answers for a display it considers invalid, and `CGRectInfinite`, whose `u32::MAX` extents would otherwise ask for a window covering the coordinate space — is refused at the source, `panel_geometry` returning `None` rather than a placeless overlay. So the failure mode left is a plausible-but-*wrong* rect, not a nonsense one, and a panel that reports none is simply un-dimmable below its floor, exactly as it was before this change. **(c)** Gamma on the built-in panel is reachable for the first time, so the macOS-gamma-sink row's advisory caveat now applies to the panel too (in practice an EDR panel refuses gamma before that matters) — and so does `MacSink::restore`, which writes a **linear identity** table rather than restoring the display's profile, on the one display whose factory calibration a user is most likely to notice. **(d)** A software-only member now pins the panel's backlight to 100 %: `fan_out_hardware` drives every hardware-capable member of a software-only group to MAX so the shared overlay is the sole dimmer, which on a laptop mirroring to a software-only projector means full backlight plus an overlay — correct by the group rule, and a battery and thermal cost that did not exist while the panel was always a singleton. **(e)** The Windows half is untouched and stays open in the v0.1.2 pure-WMI row above: WMI genuinely exposes no rectangle, which is what makes it a different problem rather than the same one |
| ~~post-v0.1.5 (#90, found while wiring)~~ | `dujactl` `backend.rs` | ~~`discover()` is `discover_ddc()` + `discover_panel()` with **no merge or dedup at all** and `open()` is **DDC-first**, so on a laptop whose panel WMI can drive the built-in panel is listed **twice**, the DDC copy is labelled `ExternalDdc`, a **serial-bearing** panel gets stamped `-slot0`/`-slot1` (so `dujactl set …-slot1` resolves to nothing and fails outright while `-slot0` writes VCP 0x10 over eDP), and `doctor`'s `ddc_count()` reports it as an external monitor~~ — **fixed in `#92`**: the CLI now mirrors the app's policy rule for rule. `map_ddc_display` is the one place that classifies a DDC entry, from `is_internal` on Windows and from a hard-coded `false` on macOS (whose backend filters built-ins with `CGDisplayIsBuiltin` and carries no such field); `merge_displays` drops an internal DDC entry only when the panel backend listed **any** panel (presence, not an id match, so the serial-less id divergence is covered) and always keeps externals and the panel rows; `merge_and_resolve_slots` runs `assign_twin_slots` on the **merged** list, so a deduplicated panel keeps the bare id `open` can actually resolve; `open` is panel-first; and `doctor`'s two counts (`external_count`/`internal_count`, relabelled `external monitors` / `internal panels`) are derived from that one merged set, so the summary can no longer disagree with the listing below it. Nine tests pin the policy. **Six of the nine were proven red** with the two historical bodies restored in place — 3 by the un-merged `discover()` and 6 by the hardcoded kind, overlapping — plus the merge-survivor choice, which a mutation keeping the DDC row instead of the native panel row reds (it did **not** before review: the assertion compared `kind`, and both candidate rows are `InternalPanel`, so only `name` discriminates them). The remaining **three are non-regression guards that were already green** against the shipped code and are not evidence of anything: `genuine_identical_external_twins_are_still_slotted`, `external_ddc_display_always_survives_regardless_of_the_panel_backend`, `ddc_display_without_a_name_falls_back_to_a_dash`. See `#92`'s body for every `left:`/`right:` | Was pure divergence from the app rather than a design gap — recorded in `#90` instead of fixed there because each consequence deserved its own red-first test and the fix is a real Windows behaviour change, not a macOS wiring detail. That is now done. The residuals are the row below |
| post-v0.1.5 (#92) | `dujactl` `backend.rs` | Three residuals of the merge fix. **(1) `open`'s panel-first order is not unit-pinned.** `open_panel`/`open_ddc` call `duja_panel::enumerate`/`duja_ddc::enumerate` directly, so there is no seam a test can observe the order through: re-inserting the historical DDC-first order leaves the whole suite green. The app has the identical gap at `open_controller`, for the identical reason, and both orders are load-bearing (DDC-first writes VCP 0x10 at an eDP panel WMI owns). **(2) The two `discover_*` arms remain hardware-only.** Everything downstream of them is pure and tested, but the `Ok(displays) => …map(…)` / `Err(_) => Vec::new()` shells, and the `d.is_internal` vs `false` argument each platform hands to `map_ddc_display`, are verified by the type checker alone — on macOS not even by a live run (see the `#90` row above). **(3) The dual-internal-panel case now bites the CLI too.** Adopting the app's coarse presence dedup adopts its residual: a second, GPU-driven internal panel that WMI cannot see is dropped from `dujactl list` on an exotic dual-panel laptop (ASUS Zenbook Duo, Lenovo Yoga Book 9i), exactly as the `v0.1.3 (#64)` row describes for the tray. The general shape of that exposure is worth stating outright, since it is new to the CLI and cuts against the project's "degrade safely — worst case present-but-suboptimal, never gone" rule: **any** `duja-ddc` `is_internal` false-positive now makes a display **vanish** from `dujactl list` (when the native backend lists a panel), where before this PR it was merely listed as external. The risk is low because `is_internal` is not a heuristic but an OS-reported connector fact (CCD `outputTechnology` ∈ {`INTERNAL`, `DISPLAYPORT_EMBEDDED`, `UDI_EMBEDDED`}, `duja-ddc/src/win/sys.rs`'s `is_internal_output`), and the tray has carried the identical exposure since `v0.1.3` — but low is not zero, and a vanished row is the failure mode the rule exists to forbid | (1) and (2) are the price of `dujactl` not depending on `duja-app`: the CLI has no fake-backend infrastructure at all, and building one into a ~0.8 MB companion binary to assert two call orderings is a worse trade than adding the seam app-side first and copying it — the same order this PR copied the policy in. (3) is deliberately *identical* to the app's residual rather than better: a CLI that disagrees with the tray about which row is the real panel is the defect this PR just fixed, so the precise per-device dedup (the PnP device-instance path shared by WMI's `InstanceName` and the DDC monitor device path) has to land in both at once |
| ~~post-v0.1.5 (#92, found in passing)~~ | `dujactl` `cli.rs`/`run.rs`/`fmt.rs` + `CONTRIBUTING.md` / `.github/ISSUE_TEMPLATE/monitor-quirk-report.yml` | ~~Both places tell users to run `dujactl doctor --report`, and there is no such flag: `parse` routes `doctor` through `end()`, which rejects any trailing argument, so the documented command exits `EXIT_USAGE` with ``unexpected argument `--report` ``. Either accept and ignore `--report`, or add the machine-readable report it implies, or fix both docs~~ — **drained in `#95`**, and the product question this row posed is answered: **option 2, add the report the flag implies**. Not option 1 (accept and ignore) — the template's own preamble promises the paste "contains the monitor identity and **probed capabilities**", and `run::doctor` called neither `probe()` nor `get()`, so an accepted-but-inert flag would have shipped a `doctor --report` that is *strictly less informative about the monitor than `dujactl list`*, and left four of the six symptoms its **required** dropdown offers (capability string wrong/missing, wrong or lying brightness range, input-source switching broken, brightness commands ignored) with no supporting evidence anywhere in its **required** textarea. Not option 3 (fix the docs) for that same reason plus one more: `CONTRIBUTING.md` calls this "the most valuable non-code contribution", so the answer to "the command does not exist" is to build it, not to stop asking for it. `--report` now opens each display and prints what the hardware answered — `Capabilities.raw_capabilities` (the raw MCCS string), the probed feature set, the live `current/max` with the `hardware_range` verdict, and `allowed_inputs` — while a probe **failure** is printed as a finding and still exits 0, because "enumerates but DDC is dead" was byte-identical to a healthy monitor in this output. Plain `doctor` stays probe-free (0 DDC reads, ~30 ms measured; `--report` is ~1.2 s for one monitor) and gains only two free lines: the `dujactl: <version> (<os> <arch>)` identity header — unconditional, since `README.md`, `docs/qa-checklist.md` and `docs/STATUS.md` all cite *plain* `doctor` as the diagnostic — and any quirk `notes`, which makes ADR-0007's "the `dujactl doctor` report can cite accumulated quirk `notes`" true for the first time (`ResolvedQuirks.notes` had accumulated them since P2 with nothing rendering them; the shipped `MSI-30B6` entry carries one, so it prints on the dev box today) | Fixed rather than carried: the docs were not wrong about what a reporter needs, only about what existed. The residual is a seam, and a narrower one than the `#92` row above: `doctor`'s assembly is now the pure `run::doctor_lines(report, reachable, displays, probe)` with the probe **injected**, so "`--report` probes, plain `doctor` does not" is asserted by *counting calls* rather than inferred from text — three mutations proven red, including the `let _ = report;` deletion that left 913/913 green before. What is still hardware-only is `probe_display`'s own `backend::open`→`probe`→`get` shell, exactly like the `discover_*` shells in the row above and for the same reason (the CLI has no fake-backend infrastructure). Exercised against the real MSI MP273QP with the tray app running: 6/6 clean runs, no contention with the engine's own writes |
| post-v0.1.5 (#95) | `dujactl` `run.rs` + `README.md` / `docs/qa-checklist.md` | There is **no** permission, kernel-module or platform diagnostic anywhere in `dujactl`: `doctor` enumerates displays and resolves quirks, and that is the whole of it. `README.md`'s support-matrix footnote nonetheless claimed `dujactl doctor` "checks" for the Linux `i2c-dev` module and udev rule, and `docs/qa-checklist.md`'s Linux section made "`dujactl doctor` names the fix" a **release gate** — one that cannot pass, since neither the check nor the Linux DDC backend exists. Both lines were corrected in `#95`; the check itself (is `i2c-dev` loaded, is the user in the `i2c` group, is `/dev/i2c-*` readable, and the one-line remedy for each) lands with the P7 Linux backend | The diagnostic's *content* is that backend's error taxonomy, so writing it first would be guessing at which failures actually occur on real X11/Wayland boxes. `#95` corrected the false claims immediately — a release gate that cannot pass is worse than a missing one — and left the feature to the phase that can define it |
| post-v0.1.5 (#95) | `duja-ui` `settings.slint` / `duja-app` `tray/wiring.rs` | The GUI cannot report its own build and offers no route to its logs. `grep -i version` across every `*.slint` finds **nothing**: the settings window's "ABOUT & UPDATES" section holds the update toggle, a Check-now button and a status label, and no version string — so a user who never installs `dujactl` has no way to answer a bug report's first question. Likewise the tray menu is exactly Open / Settings / Restore screen / Restart / Quit (+ a conditional "Update available"); there is no "Open log folder", which `.github/ISSUE_TEMPLATE/bug.yml` claimed there was. Add a version line to the About section, and a menu item or About-section button that opens the log dir (`ShellExecuteW` on the resolved `DujaPaths::log_dir`, the same call `tray.rs` already makes for the releases page) | `#95` closed the *false* half — bug.yml now asks for `dujactl version` (or `doctor`'s new `dujactl:` line) and gives the literal log path per platform — which unblocks bug reporters today without a UI change. The remaining half is real UI/tray work: a `.slint` property plus its binding test, and a menu item whose click delivery is only verifiable by hand (see the P1 row at the top of this table). Worth doing in the next settings pass, not smuggled into a CLI fix |
| post-v0.1.5 (event-loop-first) | `duja-app` `tray.rs` | **Nothing ties duja's own loop-time assembly to the mechanism it rides on.** `tests/loop_time_assembly.rs` pins the *mechanism* against the real Slint/winit stack (a zero-duration `Timer::single_shot` queued before `run_event_loop_until_quit` fires once, from inside the loop, leaving no timer entry), and `LoopAssembly`'s three tests pin the hand-back **cell**. What no test pins is the wiring between them: re-inlining `build_tray`/`init_hotkeys` back into `run`'s pre-loop phase — the exact regression this PR exists to prevent — keeps the entire suite green, because Windows tolerates the old ordering and only macOS does not. Closing it needs `build_tray`/`init_hotkeys`/`assemble_with_loop_running` behind a seam a test can observe the loop state at (e.g. an injected "is the loop running" probe, or a `TrayBuilder` trait), which is an `AppState`-shaped refactor, not a test | `AppState` cannot be constructed in a test — it owns two live Slint shells and a concrete `tray_icon::TrayIcon`, whose only constructor does `CreateWindowExW` + `Shell_NotifyIconW` (the v0.1.1 row above, same blocker). Deliberately narrowed rather than left as "untestable": the earlier version of this row claimed an ordering assertion needed a Slint harness `duja-app` does not have, which was **wrong** — `duja-app` depends on both `slint` and `duja-ui`, so it can and now does drive a real winit-backed loop in an integration test. Only the duja-specific half remains, and it is the half that needs the refactor |
| post-v0.1.5 (event-loop-first) | `duja-app` `tray.rs` | Two residual paths around the loop-time assembly, both currently unreachable. **(1)** A `slint::run_event_loop_until_quit()` that fails to **start** is logged and still exits 0 (pre-existing); after the restructure that same path also implies the queued assembly never ran, so there is no tray either — the "running-but-invisible app" shape, reached by a different route than a tray-build failure (which is properly fatal). Report it as a fatal startup error instead. **(2)** `build_tray`, `init_hotkeys`, `StateStore::load` and `wire_event_sources` now run inside a Slint timer callback, so a panic in any of them unwinds through `TimerList::maybe_activate_timers` — leaving its `callback_active` slot set, so the next `update_timers_and_animations` trips Slint's own `assert!(…, "Recursion in timer code")` — and then into winit's `catch_unwind`, instead of out of `run()`. `main.rs` already anticipates a panic inside a Slint callback aborting the process, so the outcome is a crash-file entry rather than silence, but the abort surface is wider than it was | (1) is unreachable in practice: winit dispatches `StartCause::Init` on the loop's first pass on both platforms (Windows `runner.rs` `move_state_to`, macOS `app_state.rs` `dispatch_init_events`), and that is what fires the closure — so the only way in is the loop refusing to run at all. Flipping an exit code is a real behaviour change and was kept out of a restructure whose contract was "the existing suite passes unchanged". (2) is theoretical under the lint wall (no `unwrap`/`expect`/`panic!` in production code) and is the unavoidable price of running the assembly in-loop at all — every candidate mechanism (timer, `invoke_from_event_loop`, a custom application handler) puts the work inside a callback. Both belong to the next `tray.rs` change that is allowed to alter behaviour; the macOS assembly is the natural home |
| post-v0.1.5 (#95 smoke, 2026-07-30) | `dujactl` `run.rs` `doctor --report` + `duja-app` `worker.rs` | **`doctor --report` contends for the monitor's I2C with the running app, and can leave the display falsely marked `software_only`.** `--report` opens its own controller and probes over DDC by design (that is the point of it), so with the tray app up two processes drive one monitor. Observed on the dev box during the `#94` smoke: repeated `--report` runs left `dujactl list` reporting `mode = sw` for the MSI MP273QP while hardware writes kept working throughout (`set … brightness 95` moved it and `get` read it back), i.e. a false positive of exactly the class `#59`'s retried verify-first-write and `#78`'s probe fix were built to prevent. The CLI's own runs were all clean, which is why `#95` did not catch it: it verified the wrong side of the contention. Self-heals via the poll-driven `clear_software_forced` once the panel is observed at Duja's last-written level, or immediately on a fresh worker (restart/replug) | Two candidate fixes, both cheap, neither obviously right yet: have `--report` prefer the **IPC** path when a server is reachable (the app already owns paced access, and `list`/`get` already do this) and probe directly only as a fallback; or keep probing directly but **say** it is about to contend. The first is better for the user and worse for the diagnostic — an IPC-served report describes the app's view rather than the raw hardware, which is precisely what a quirk report wants to avoid. Needs a decision, not just code, so recorded rather than guessed. Note the app-side half may deserve its own hardening: a *transient* contention timeout arguably should not be enough to set a sticky flag |
| post-v0.1.5 (gamma refusal fix) | `duja-core` `dimmer.rs` / `duja-dimmer` | `GAMMA_FLOOR = 0.3` is **unreachable on Windows**, so the constant's cross-platform meaning is now uneven. Windows' `SetDeviceGammaRamp` validation refuses any ramp deviating more than 32768 (of 65535) from the identity, which puts its lowest accepted factor at `0.5` (`duja-dimmer`'s `MIN_ACCEPTED_GAMMA`, derived and measured — see the constant). macOS is **not** the same: `CGSetDisplayTransferByFormula` takes an arbitrary `(min, max, gamma)` triple and will accept `max = 0` (a fully black display), so it imposes no anti-lockout clamp and `GAMMA_FLOOR` is genuinely reachable there — the reason that is safe on macOS is crash *recovery* (the window server restores a process's transfer tables on exit), not validation. So the same `f32` means "the darkest ramp we allow ourselves" on macOS and "a value the OS refuses outright" on Windows, and only `min_gamma_factor()` distinguishes them. Consider splitting the two ideas: a per-platform *reachable* range alongside the cross-platform *safety* floor | The uneven meaning is now *named and honest* (`min_gamma_factor()` exists precisely so a caller asks before attempting a doomed write, and `dimming::plan` consumes it), so nothing is silently wrong. Renaming or splitting `GAMMA_FLOOR` touches `duja-core`'s public dimming vocabulary — every backend, `clamp_gamma`, and `DimCommand::sanitized` — for a clarity gain, not a behaviour fix. Do it with the **app-side** macOS gamma sink, so the second real consumer is present to design against rather than guessed at. (The macOS *backend* already exists — `duja-dimmer/src/mac/gamma.rs` is a real 220-line `CGSetDisplayTransferByFormula` implementation, which is how its no-clamp behaviour above is known. What is missing is the app-side `GuardSink` twin that would drive it, per the `#90` row.) |
| post-v0.1.5 (gamma refusal fix) | `duja-dimmer` `win/gamma.rs` + `duja-app` `gamma.rs` | **A refused ramp still has no feedback path, and Windows has a refusal shape that reports success.** Two residual routes to "nothing dims", both narrower than the fixed one but both real. **(1) No feedback.** The planner pre-empts the *known* refusal (a factor below `MIN_ACCEPTED_GAMMA`), but a refusal for any *other* reason — a driver that rejects gamma outright, a GPU with a tighter rule, or `resolve` returning `None` because the id has no GDI device — still leaves `cmd.gamma = Some(f)` with `overlay_alpha` 0, the coordinator retrying every batch, and nothing dimming the display. Exactly the reported shape, now with one honest warning instead of 349, but still no overlay. **(2) The documented silent refusal.** [`SetDeviceGammaRamp`](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/nf-wingdi-setdevicegammaramp) is documented to *"fail silently (that is, it returns TRUE, but it doesn't set your ramp)"* when a ramp violates its heuristics. On such hardware `GuardSink::engage` returns `true`, the coordinator records a ramp that is not live, and nothing dims **with no log line at all** — so `GammaSink::engage`'s contract is deliberately worded as "the OS *reported* the write as accepted", which is all this implementation can honestly claim. The cure for both is the same and is already this project's idiom: **verify by readback** — `GetDeviceGammaRamp` after the write, compared with tolerance — which ADR-0002 mandates for DDC writes for precisely this reason (hardware that lies about accepting a value). A failed verification then feeds the planner the same way `is_hdr_active` does, closing (1) as well | Not implemented here, for two reasons that are about evidence rather than effort. The comparison needs a **tolerance policy tuned on hardware**: the same Microsoft page notes the hardware LUT may be lower precision than the 16-bit values written, so an exact compare would false-positive and a guessed tolerance could mask a real failure — and the only display known to exercise any of this is the reporter's, which is not going to be dimmed again for a reading the fix does not need. Feeding the result back into `dimming::plan` is also a second, larger change (a per-display runtime verdict threaded through planning, like the HDR verdict), and the shipped fix already removes the failure mode that actually bit a user. Land the readback and the feedback path together, with a display that reproduces shape (2) |
| post-v0.1.5 (gamma refusal fix) | `duja-app` `dimming.rs` + `duja-ui` `settings.slint` | The overlay substitute delivers the same **level** as the refused ramp but **not the same coverage**. A gamma ramp is applied by the display pipeline, so it dims the whole scanout — including the mouse cursor, exclusive-fullscreen apps, the Start menu and notifications — and does not appear in screen captures. The overlay is a window: per ADR-0003 it dims the desktop, apps and taskbar but **cannot** cover exclusive-fullscreen, the secure desktop (UAC/lock), later-created topmost windows, or the cursor, and is deliberately excluded from captures (`WDA_EXCLUDEFROMCAPTURE`). So on Windows a `dim_mode = "gamma"` display silently changes which surfaces are dimmed at slider ≈ B/2. The **cap itself** is now disclosed in the settings window (a caption under the dim-mode row). ~~That caption is a **hardcoded string**: `duja-ui` has no dependency on `duja-dimmer` or `duja-platform`, so its "50%" is not derived from `MIN_ACCEPTED_GAMMA` and is shown on every platform even though the cap is Windows-only.~~ — **fixed in `#103`**. ~~Two follow-ups: gate the caption per platform (a `gamma_capped: bool` on `MonitorSection`/`SettingsMonitorData`, plumbed like `gamma_available`), and decide whether the figure should be plumbed rather than duplicated~~ — **both done in `#103`**, and the answer to the second was *plumb*, which subsumed the first. `MonitorSection` / `SettingsMonitorData` carry a `gamma_cap_pct` (`Option<u8>`, 0 across the Slint boundary since Slint has no optional type) sourced from `dimming::gamma_cap_pct_for_platform()` — the same seam as `plan_for_platform`, so the *choice* of `duja_dimmer::min_gamma_factor()` over a literal is pinned by a test rather than living at an `AppState` call site. A bool would have gated the caption while leaving the `50` duplicated; a number gates it for free, because "no cap" and "nothing to disclose" are the same fact. The copy is now "at most {}% **on this system**" rather than "on Windows": the platform is carried by whether the caption appears at all | ~~The user's product decision (2026-07-30) was **keep `dim_mode = "gamma"` and disclose the cap**, so the caption shipped with this fix as the copy-only half. The remaining half is not copy: a per-platform gate means a new field through the VM struct, the Slint struct, `build_section` and `monitor_to_data`, and plumbing the figure means either a new `duja-ui` dependency or passing it in from the app — neither belongs in a fix PR. It is also currently *inert*: the settings window is `cfg(windows)`-only (the tray owns it), so every user who can see the caption is on Windows and it is correct for all of them. Do the gate in the same pass that un-gates the tray for macOS, which is when it first becomes wrong~~ — **all superseded by `#103`**, which did the gate one PR after the un-gating (`#102`) rather than in it. The "currently inert" note stopped being true at `#102`, when the settings window went live on macOS; that is precisely why the caption became the last code item before packaging. ~~**One thing this row did not anticipate is still open**: the caption's "%" is the gamma *factor*~~ — **settled at the P6 gate; the user's call was to drop the figure.** The problem was real: `map_user_level` maps the sub-floor zone `0..transition` onto the whole factor range, so on a display with a backlight the factor is `user_pct / transition`, and with shipped defaults (`transition` = 25) a "50 %" cap means the substitution happens at slider **12**. The number was right and unhelpfully misreadable — a percentage beside a slider reads as a slider position. The caption is now *"Gamma can only darken this display so far on this system; past that Duja uses the overlay, which cannot cover full-screen games or the mouse pointer"*. **`gamma_cap_pct` stays plumbed**: it no longer appears in the text but still *gates* whether the caption is shown at all, which a bool could not do — "no cap" and "nothing to disclose" are the same fact. The binding test now asserts the caption contains **no digit**, which is the property that breaks if someone re-interpolates it | Nothing carried. The alternative (keep the number, name what it is a percentage *of*) was rejected because every honest expansion is either vague or a new false claim: a gamma factor is not a percentage of brightness |
| post-v0.1.5 (gamma refusal fix) | `duja-app` `tray/state.rs` `apply_overlays` | The **batch ordering** — engage new ramps → overlay diff → restore stale ramps, which exists so a mechanism switch mid-drag dips rather than flashes bright — is pinned inside `gamma::apply_dimming_batch` (three tests, all lanes), but nothing pins that `apply_overlays` routes through it. Re-inlining `dimmer.apply(&commands)` followed by the two gamma phases at that call site restores the flash with the whole suite green. Narrower than the pre-fix version of this row (the *choice of gamma minimum* is now pinned by `plan_for_platform`, so the remaining gap is the choice of **entry point**, not of a value) | `AppState` cannot be constructed in a test — it owns two live Slint shells and a concrete `tray_icon::TrayIcon` whose only constructor does `CreateWindowExW` + `Shell_NotifyIconW` (the same blocker as the v0.1.1 throttle row and the `#94` loop-assembly row). Mitigated the way those are, and one step further than they can be: the sequencing has no alternative spelling at the call site — `GammaBackend` no longer exposes a gamma-only `apply` at all, so the wrong order cannot be written without also re-adding a method — and both the method and the call site carry the reason in prose |
| P6 (macOS surface token) | `duja_core::macos` (was `duja-ddc` `mac_surface.rs`) + `duja-app` `backend.rs` + `duja-panel` | The macOS display-surface token collapses a mirror set onto the master's `CGDirectDisplayID` (`CGDisplayMirrorsDisplay(id)` when non-zero, else `id`), which is what discharged the `#90` standing rule and let `clone_group` run on macOS. The **rule** is proven (`duja_core::macos`'s tests run on every CI lane); the **premise** is not: nothing has ever observed a real macOS mirror set, so whether `CGGetActiveDisplayList` reports every member of one — or only the master — is assumed, not known. Confirm on hardware, and while doing so settle the coverage question the split leaves open: a mirrored set gets **one** `DimCommand` for the shared surface, so a gamma engage reaches the anchor member's transfer table alone. On Windows that is complete by construction (one GDI device covers the clone set); on macOS each display owns its own table, so the other members of a mirrored group keep identity gamma and stay brighter. **`#105` widened the same unverified premise rather than changing it**: the built-in panel now reports a surface token too (from this very rule, via `duja_core::macos`), so a mirror set can span both backends and the panel can be a *member* of a group where before it was always a singleton. Every word above still applies to it — plus one thing the deferral note in the next column does **not** cover and which `#105` newly introduced. The "deliberately not load-bearing" argument is about *grouping* and *gamma addressing*; **placement** is a third consumer. A group's single `DimCommand` takes its bounds from the anchor, the anchor is the lowest id string, and an Apple panel's `APP-…`/`AAP-…` sorts ahead of nearly every monitor PNP id — so in the commonest mirror layout the overlay is now positioned from the panel's `CGDisplayBounds`, taken from the *online* display list rather than the active one this backend uses. A wrong rect there mis-covers both screens at once, which grouping alone never could | Was deliberately not load-bearing in either direction, which is why it shipped — `#105` added a third direction and that is no longer the whole story, so read this with the **placement** paragraph opposite. For **grouping**: if only the master is enumerated, every surface token equals its own display id, `group_clones` builds exactly the singletons it builds today, and behaviour is unchanged — the assumption can only *add* a merge, never remove one. For **addressing**: nothing rides on it at all any more, because gamma resolves through `gamma_token_for` (this display's own id) rather than the surface token — the review of the one-token version found the case that forced the split, a `MacBook` mirroring its built-in panel to a projector, where the master is the display `CGDisplayIsBuiltin` filters out and a single token would have dimmed the laptop screen instead of the monitor. For **placement** (new in `#105`): it *is* load-bearing, and nothing in this row bounds it — a mirrored group's one overlay is sized from the anchor's bounds, and the anchor is now usually the panel. That is the half a Mac has to check first. The remaining gamma-coverage half cannot be designed blind: whether a hardware mirror even exposes a non-anchor clone as gamma-addressable is what decides between "fan the ramp out to every member" and "document it as a limit", and guessing would risk writing transfer tables to displays that are not drawable. Needs the same Mac as the `mac/sys.rs` rows above |
| P6 (macOS gamma sink) | `duja-app` `gamma.rs` + `duja-dimmer` `mac/gamma.rs` | **`CGSetDisplayTransferByFormula` reports success without applying the ramp, and the Windows cure does not transfer.** Apple's forums carry a DTS-acknowledged report that the call returns `kCGErrorSuccess` while *"the display's actual gamma curve remains unchanged despite the API reporting successful completion"*, with a trigger as ordinary as leaving **"Automatically adjust brightness" on** — the default on Apple Silicon laptops — plus a broader recurrence on recent hardware (M5 Pro/Max/Neo, Tahoe 26.3.1+). f.lux, Lunar, `BetterDisplay` and `MonitorControl` are all affected. Consequence for Duja once the tray is un-gated: `MacSink::engage` gets `Ok(())`, returns `true`, the coordinator records the factor, and a gamma-mode display has `overlay_alpha == 0` by construction — so **nothing dims, with no log line and no retry**. That is the `#96` shape, but worse: on Windows the refusal at least returned `FALSE` and produced one honest warning. The recorded remedy for the Windows twin — verify by readback (ADR-0002) — **provably does not work here**: on the reported hardware `CGGetDisplayTransferByTable` returns exactly the values just written while the screen is unchanged, so a readback would confirm a ramp that is not live | No local cure is available and the remote one is Apple's. What Duja can do is bound the damage, and the options need a Mac to choose between: (a) treat macOS gamma as advisory and plan the overlay **instead of** it, which costs the gamma path its only advantage — note *alongside* would be a bug, not a conservative choice: a display would take the ramp AND the alpha and land far darker than asked; (b) detect the auto-brightness setting and refuse gamma while it is on, which is a private-API guess; (c) ship gamma on macOS as opt-in with the hazard disclosed in the settings copy, mirroring the Windows 50% cap decision the user already made. (c) is the cheapest and most consistent, and is what the un-gating PR should implement if no Mac appears first. Note the sink is currently unreachable anyway on any Mac with an EDR panel — `gamma_support_from_hdr` refuses gamma there — which is most MacBook Pros since 2021. **`#103` implements (c).** The settings window now carries a second caption under the dim-mode row — *"Gamma may not take effect on this system: macOS can accept a ramp and not apply it, most often while "Automatically adjust brightness" is on. If the screen does not dim, use the overlay instead"* — driven by a new `duja_dimmer::gamma_is_advisory()` and plumbed through `GammaLimits` beside the cap. It is the same product decision the user made for the Windows cap on 2026-07-30: keep `dim_mode = "gamma"`, disclose the limit. **The first draft of `#103` deferred this on a false argument, and the correction is the useful part of the row.** That draft claimed the row above (`SetDeviceGammaRamp` *"returns TRUE, but it doesn't set your ramp"*) is *"the same shape on Windows"*, so any plumbed `gamma_may_silently_fail` would be `true` on both platforms and (c) would put a hazard caption in front of every Windows user. **The review disproved it from the quoted source.** Microsoft's own sentence carries a conditional the draft dropped: the function *"implements heuristics to check whether a provided ramp will result in an unreadable screen. **If a ramp violates those heuristics**, then the function fails silently"* — and the heuristic is quantified on the DXGI page as *"any entry in the ramp must be within 32768 of the identity value"*. `MIN_ACCEPTED_GAMMA` is derived from exactly that bound, is pinned **at** it by `win/gamma.rs`'s tests, and the planner substitutes an overlay below it — so every ramp Duja sends complies by construction. `MIN_ACCEPTED_GAMMA`'s own doc had already said as much (*"staying at or above it is the only reliable protection"*), which the draft contradicted without noticing. So the two differ in **mechanism**, not likelihood: Windows states a rule and Duja satisfies it; macOS fails on *valid* triples with no rule to satisfy and no readback that detects it. That asymmetry is what made the disclosure genuinely macOS-only **when this row was written** — and it removed the argument for deferring, so the deferral went with it. **`#124` changed that**: Linux X11 is now advisory too, for a third mechanism (`ProcRRSetCrtcGamma` discards the driver hook's result and answers `Success` regardless), so `gamma_is_advisory()` is true there and the caption's macOS-specific wording is owed a rewrite — see the row below. Residual on the Windows side is unchanged and stays in the row above: a driver or GPU with a tighter, undocumented rule is a hardware unknown, not a mode the platform has, and `gamma_is_advisory` says so in its doc rather than claiming Windows is safe. **Still open here**: options (a) and (b) — planning the overlay instead of the ramp on macOS, or detecting the auto-brightness setting — both of which need a Mac to choose between, and either of which would supersede the caption |
| P6 (macOS gamma sink) | `duja-app` `gamma.rs` + `duja-dimmer` `mac/gamma.rs` | **The no-crash-marker design rests on an undocumented belief, and there is no fallback if it is wrong.** Windows keeps a marker file because its ramp outlives the process; macOS omits all of it on the grounds that the window server restores a process's transfer tables when it exits. That is widely observed, but Apple documents it **nowhere**: `CGDirectDisplay.h` says nothing about restore-on-exit, the one official sentence that sounds like it (*"When your application terminates, the display arrangement returns to the current settings in Displays preferences"*) is about display *configuration*, Apple's own `MacGamma` sample saves and restores explicitly, and Gamma Control's changelog records moving to reset on quit *"instead of counting on macOS to do it for us"*. The hard-kill case (SIGKILL, force-quit) is assumed, not verified. If it is wrong on some configuration the failure is silent and total: nothing writes the marker, so `startup::recover_from_crash_marker` can never fire, and the only recovery is a user finding `duja --restore` on a screen they may not be able to read. **Also in this row: the two restore paths leave different states.** `restore_all` goes through `CGDisplayRestoreColorSyncSettings` (the user's profile); the per-display `restore` writes linear identity, so a *calibrated* display that leaves the sub-floor zone comes back un-dimmed but un-calibrated until the next global restore | Insurance exists and needs a Mac to justify its cost: write the marker on macOS too and let the existing recovery path run. The *recovery* half really is free — `startup::recover_from_crash_marker` is cross-platform and `tray.rs` already calls it unconditionally — but the writing half is not one line: `mark_dirty`/`clear_marker`/`marker_present` live inside `duja-dimmer`'s `#[cfg(windows)] mod win` and are re-exported only from the Windows block, so they do not exist on macOS at all. They are pure `std::fs`, so hoisting them is easy, but it is a two-crate change. Deliberately not done blind, because on the overwhelmingly likely reading (the belief is correct) it adds a file write per engage and a startup stat for nothing. The per-display divergence has a better third option than the row first claimed, and the claim that there was none was wrong: **snapshot-and-restore**, which is exactly what Apple's `MacGamma` sample (cited above for the restore-on-exit question) demonstrates — `CGGetDisplayTransferByTable` before Duja's first write, `CGSetDisplayTransferByTable` to put it back. Per-display, no flicker, preserves calibration, and both symbols are already in the bound `objc2-core-graphics` surface under features this crate enables. Its real costs are two new `unsafe` blocks with pointer out-params in a backend that currently has one, and a `CGDisplayGammaTableCapacity` query plus a 3xN `CGGammaValue` buffer per engaged display — where that query can answer **0** for a display with no gamma table, so option 3 needs the current identity write as its own fallback rather than replacing it outright. Two residuals: another app changing the table mid-session gets Duja's older snapshot back (still better than identity, which clobbers it unconditionally), and it narrows the two-paths-two-states divergence rather than closing it, since `restore_all` would still reload the profile while this writes the snapshot. Deferred because writing that FFI blind, for a path unreachable on any EDR Mac, is not a trade to make without hardware |
| P6 (macOS tray) | `duja-app` `tray/icon.rs` | The macOS status icon is **not** a template image (`tray-icon`'s `icon_is_template` stays `false`), so it keeps its accent colour instead of adopting the menu bar's foreground. That is deliberate — the accent is the icon's identity and is user-configurable, and template rendering would discard it — and the obvious risk is already covered: `duja-ui`'s `icon_rgb_reads_on_both_taskbars` asserts all five accents clear 3:1 WCAG contrast against both a light and a dark chrome colour, which bracket the macOS menu bar in both appearances, and that test runs on the macOS lane. **What is not covered is the highlight state.** `tray-icon` calls `highlight(true)` on click and leaves it set for as long as the context menu is open (`menu_on_right_click` is still `true`), and `AppKit` paints its standard selection fill behind the button. A template image inverts to white over that automatically; a coloured glyph does not, so the accent sits on a selection background at whatever contrast it happens to have. No test models this and no rule predicts it | Hardware-blind and cosmetic, so it is recorded rather than guessed at. Whoever first runs Duja on a Mac should look specifically at **the icon while the right-click menu is open**, in both light and dark appearance, not just the idle icon — the idle case is the one already proven. If it reads badly the fix is a decision, not a port: either accept template rendering and lose the accent on macOS, or keep the colour and accept the highlight |
| ~~P6 (`#103` CI)~~ | `duja-app` `tests/engine.rs` | ~~**`worker_panic_does_not_kill_engine` is timing-flaky on the Windows CI lane.**~~ — **diagnosed at the P6 gate: a test artifact, not a dropped notification.** The row demanded the distinction be *established* rather than papered over with a bigger constant, and it now is. Evidence: **3,014 runs, 0 failures** across four harnesses (50 under nextest; 2,000 direct at 8-way; 960 with `RUST_BACKTRACE=1` at 24-way against 32 CPU burners on 16 cores; 4 full-workspace runs at `--test-threads 32` under load). One measurable cost sits inside the budget: Rust's default panic hook runs **at the panic site**, inside `catch_unwind` in `worker.rs` and *before* the `Panicked` ack is sent, so the budget absorbs a dbghelp symbolization of the test binary's PDB — ~30 ms idle, ~85 ms with `RUST_BACKTRACE=1` (which `ci.yml` sets), 184–201 ms under 32-way load. **It is nowhere near large enough to be the cause, and this row is the record of that**: across **four** red CI runs the failing waits took 2.804 s (run 30504679809, `04a074f`, 2026-07-30 — the one this row originally missed, and the only one never re-run), 10.266 s (30597183871), 6.589 s (30620730412) and 2.850 s (30700482072). Two orders of magnitude are missing, so anyone reaching for the hook as *the fix* is reading this row wrong — see the fourth-run entry below, where exactly that mistake was made and caught in review. The two 2026-07-31 jobs (30597183871 and 30620730412) also failed `install-action`'s bash startup, corroborating a sick runner, and both were green on a re-run of the identical commit. Nothing in production depends on 2 s: the real backstop is `watchdog_timeout` = 5 s, and `.config/nextest.toml` now carries a hang guard | **Both halves are now done (`#113`), and the fourth red run is why.** Run 30700482072 failed this test at 2.850 s on `f96e4bd` — a **docs-only** commit. Re-running the identical commit turned it green at **0.069 s**, the same outcome as the two earlier re-run reds. (Three of the four reds were re-run and all three went green; 30504679809 never was. That matters: it is the only one of the four whose outcome could have contradicted the environmental reading, and at 2.804 s it is the near-twin of this one — two failures at ~2.8 s is a tighter signature than the 6.6/10.3 s outliers, and this row missed it until review caught the miscount.) What makes this one decisive is a test with **no panic in it at all**: `loop_time_assembly`'s zero-duration single-shot went 0.363 s green → **4.308 s** red → 0.777 s on the re-run, while the median test in the red run was 1.04x its green time. A transient stall that hits an unrelated event-loop test twelvefold cannot be bounded by anything on the panic path. (One prior corroboration did *not* recur: this job's `install-action` succeeded, so "sick runner" rests on the re-run and the cross-test pattern rather than on a second failing step.) Done: `LIVENESS_BUDGET` (10 s) now names **every positive wait** in `tests/engine.rs` — the 53 that were on a 2 s budget, plus `drain_writes`' shared 3 s and the ~20 stragglers at 1 s, 3 s, 4 s, 5 s and 8 s. Those last ones matter and were nearly left behind: several sat *below* the 4.3 s stall the constant exists to tolerate, so "fixing" only the 2 s sites would have left the file's next-most-fragile waits more exposed than the one that failed. The **14** negative waits — which elapse in full every run — deliberately keep their own short literals (500-700 ms, 1 s, one at 2 s), each with a note. The no-op hook is in too, but as a **cleanup worth ~50 ms, not the fix**, and it keeps the panic's message and location so the next failure still shows that a worker panicked. Recorded because the first draft of `#113` got this backwards and review blocked it: it claimed `RUST_BACKTRACE` was "the single environmental difference", which **this row already refuted** — 960 of the 3,014 control runs had it set and passed, and five green Windows CI runs paid the same symbolization at 0.057–0.184 s. `std::panic::set_hook` is still **process-global**, which is why the installer is prefix-scoped, `Once`-guarded and documented as a one-way door |
| P6 (`#104`) | `xtask` `bundle.rs` + `duja-ui` `icon.rs` | **`Duja.app` ships without an icon.** There is no `CFBundleIconFile` and no `Contents/Resources/*.icns`, so Finder, the mounted disk image, the Login Items list, Force Quit and the app switcher show the generic application icon. The *tray* icon is unaffected — that is drawn at runtime from `duja-ui`'s `monitor_rgba` — so what a user actually looks at all day is correct; this is the icon on the container | The art has no raster form: `icon.rs` renders the silhouette in code and **no PNG/ICO asset for it exists in the tree** (`crates/duja-app/assets/duja.ico` is a separate hand-made installer icon). Producing an `.icns` therefore needs one of three things, none of which belongs in a packaging PR: a PNG encoder inside the deliberately dependency-free `xtask` (hand-rolling one is ~60 lines of stored-deflate + CRC32, testable but real code); an `xtask` dependency on `duja-ui`, which would pull Slint into the build tool; or a macOS-only `sips`/`iconutil` pipeline in the workflow, which **cannot be verified without a Mac** and would make the icon exist only on CI-built artifacts. Prefer the first — it keeps the icon derived from the same `monitor_rgba` art as the tray, and it is unit-testable on every lane. Degrades exactly as the standing rule requires: present but plain, never vanished |
| P6 (`#104`) | `.github/workflows/` | **The macOS packaging path has no PR-time coverage.** `lipo`, `codesign` and `hdiutil` exist only on macOS, so `ci.yml` cannot run them; the only automated exercise is `release.yml`'s `workflow_dispatch` dry run, which a human has to trigger. A change that breaks bundling is therefore green on every PR and red only at release time | Deliberate cost trade, and the exposure is bounded rather than open: every *decision* the packaging makes — the `Info.plist`, the bundle layout, the artifact names, the version alphabet, the host→target mapping — is pure code in `xtask`'s `bundle`/`version` modules and unit-tested on all three lanes, so what is uncovered is the four `Command` invocations and their argument spelling. Covering those in `ci.yml` means two release builds of `duja-app` on a mac runner (~10 min of runner time) on every PR that touches anything, to re-answer a question that changes only when `xtask/src/dist.rs` does. If it becomes a recurring break, gate a `packaging (macos)` job on `paths: [xtask/**, .github/workflows/release.yml]` rather than running it always |
| P6 (`#104` review) | `xtask` `bundle.rs` + `.github/workflows/release.yml` | **`LSMinimumSystemVersion` is 11.0, which excludes Intel Macs that cannot run Big Sur.** A universal binary records a deployment target *per slice* and Launch Services gates launch on this key alone, so the bundle could honestly advertise 10.13 with an `x86_64` slice built for 10.13 — the `arm64` slice's own floor is never consulted on hardware that could not use it. The review corrected a claim in the first draft that said otherwise | Lowering it is not free: it needs a **per-arch** `MACOSX_DEPLOYMENT_TARGET` (one `cargo build` step per floor), a per-arch expectation in `Verified::checked` instead of one constant, and — the real cost — an assertion of support for releases **no lane tests and nobody has run Duja on**. 11.0 is the lowest floor both slices share from a single setting. The excluded population is Macs older than roughly 2013–2014, not the Intel population whose DDC confirmations the `duja-ddc` `mac/` row is waiting on — those all run Big Sur. Revisit if a community report actually arrives from an older Mac; that is the evidence that would justify the extra machinery |
| P6 (`#104`) | `xtask` `bundle.rs` + `packaging/macos/` | **`dujactl` is not on `PATH` for a macOS user.** It ships inside the bundle at `Duja.app/Contents/MacOS/dujactl`, so the CLI works only by full path. The Windows portable zip puts both binaries side by side at the archive root, where the user can add one directory to `PATH` | The DMG is a drag-to-install: it touches `/Applications` and nothing else, which is precisely why it needs no installer and no elevation. Putting `dujactl` on `PATH` means writing to `/usr/local/bin` — outside the bundle, from something that is not an installer, and a file no uninstall would ever remove (dragging `Duja.app` to the Trash is the whole uninstall story on macOS). The candidates are a Homebrew cask with a `binary` stanza (the idiomatic answer, and it also solves updates), or a first-run offer from the app itself. `packaging/macos/README.md` documents the one-line `ln -s` meanwhile |
| P6 (`#104` review) | `duja-platform` `autostart/mac_location.rs` + `duja-app` `tray/state.rs` | **The mounted-volume autostart refusal has no user-visible explanation.** Enabling launch-at-login from a copy running inside the mounted `.dmg` now fails with a message naming the remedy, but `apply_autostart` only `warn!`s it and re-reads the real state — so the user sees the toggle spring back with no reason given. Without the refusal the login item would be silently dead forever, so this is strictly better; it is still a setting that refuses without saying why | Surfacing it needs an error channel from `Autostart` to the settings window that does not exist yet — today `SettingsVm::set_general` carries state, not diagnostics — and the copy would have to be translatable and platform-specific. The same gap applies to every other `AutostartError` (a registry denial on Windows is equally silent), so the fix is one general mechanism rather than a special case for this one, and it belongs with the settings-diagnostics work rather than in a packaging PR |
| P6 (`#113` review) | `duja-app` `bin_support/gamma.rs` + `duja-dimmer` `win/gamma.rs` | **A failing `CreateDCW` against a deliberately bogus device name costs seconds on the Windows CI runner and milliseconds locally, and six tests pay it — ~138 s of runner time per run.** Measured on run 30700482072 attempt 2: `a_refused_ramp_is_retried_but_latched_so_it_warns_once` **88.705 s** (82.149 s on run 30700246963) against **0.076 s locally**, plus five one-call siblings — `a_refused_ramp_reports_failure_and_is_not_recorded_as_engaged` 10.383 s, `first_gamma_engage_writes_marker_and_clean_quit_clears_it` 5.415 s, and `duja-dimmer`'s `drop_keeps_marker_when_a_restore_fails` 12.546 s, `restore_now_keeps_marker_when_a_restore_fails` 10.888 s, `restore_display_keeps_display_and_marker_when_restore_fails` 10.077 s. Only the first trips nextest's `SLOW [> 60 s]`, and it does so on **every** Windows run. **The per-call cost is inferred, not isolated**: fitting `cost = fixed + n x marginal` across the 1-call and 12-call cases gives a **marginal ~6-7 s** with a **fixed ~3-8 s** that the one-call tests also pay, so the obvious `88.705 / 12` is wrong — it assumes the fixed part is zero, and it is roughly the size of one call. By `.config/nextest.toml`'s own rule this is "a finding, not a threshold to raise", and at ~88 s the worst test sits within ~2x of the 180 s `terminate-after`: one of the runner stalls in the `#103` row above could push it over and turn a green suite red for a reason unrelated to any defect. Preferred remedy, because it fixes **all six**: inject a fake `CreateDCW` failure rather than provoking a real one (the sink already has a seam for the *refusal record*, just not for the device open). Cutting the 12 attempts helps only the one test and, on the fit above, would still leave it near 15-20 s | Not a product defect — the code under test is correct, and the dominant cost is the CI runner's Win32 behaviour for a bogus device name, which is exactly what these tests mean to exercise. What is *not* established is that it is the only cost: the fixed component was inferred from two data points, never measured per call, and nothing here rules out contention from the other five tests running concurrently. Recorded rather than fixed because `#113` was already a timing PR with a blocked first attempt, and folding an unrelated six-test rewrite into it would repeat the mistake the `#103` row warns about. Surfaced by the `#113` review while checking a claim of mine that leaned on the nextest backstop |
| P7 wave 0 (`#114` review 3) | `duja-platform` `ipc/unix_socket.rs` | **`takeover_bind`'s liveness probe is the one unbounded wait left in a module that bounds everything else, and `#114` widened its blast radius.** The probe is a plain blocking `UnixStream::connect`, and against a *live* server whose listen backlog is full the two platforms fail differently. On the BSDs `connect` returns `ECONNREFUSED`, so a live server is misread as stale and its socket is unlinked - the exact defect the rest of that module exists to prevent, reached by a route the bind lock cannot see. On Linux `unix_stream_connect` finds the receive queue full and waits with no send timeout for an `accept` that may never come. Reaching either needs enough queued connections from a process of the **same uid** to fill the backlog — std passes `listen(fd, -1)`, so that is `somaxconn` on Linux (4096 by default since 5.4) and 128 on Apple, not the "~128" this row first claimed for both — which is inside the trust boundary and so is self-harm rather than an attack. What `#114` changed is that the probe now runs while holding the bind lock, so the Linux case escalates from "this instance stalls" to "every concurrent starter also times out at `LOCK_WAIT`". Remedy: a non-blocking `connect` plus the `poll_ready` helper the module already has, with the same slice/deadline shape as every other wait here; the `ECONNREFUSED` arm additionally wants an `fstat` owner check before believing a refusal | Recorded rather than fixed because it is a **different mechanism** from the locking change that surfaced it, and `#114` had already grown from one fix to three across two review rounds - folding in a fourth is the mistake the `#103` row warns about. The probe itself predates `#114` and is unchanged; only its scope under the lock is new. Not reachable without a same-uid process deliberately filling a 128-deep backlog |
| P6 (`#104` review 2) | `xtask` `macho.rs` | **No test constrains the Mach-O constants against bytes Apple produced.** The fixtures build synthetic binaries using the *same* constants the parser compares against, so a wrong `LC_BUILD_VERSION`, `MH_MAGIC_64`, `CPU_TYPE_*` or field offset would leave the suite green while making `Verified::checked` reject every real universal binary — and since that runs inside the packaging step, the failure direction is **every release blocked**. The workflow's `otool` cross-check cannot cover it either: a false reject kills the Package step, so the Verify step never runs to disagree | Not deferred for cost — deferred for **access**. Closing it means checking in a captured real fat header (the first few KB of a `lipo` output is enough), and producing one needs a Mac: the release artifact is a UDZO/HFS+ disk image, and the Windows dev box has no archiver installed that can extract one (7-Zip lists DMG/HFS among its formats, so this is a missing tool rather than a missing capability). What holds meanwhile is weaker but not nothing: every constant was read off Apple's `cctools` (`fat.h`, `mach-o/loader.h`, `libstuff/ofile.c`) and dyld and is cited at its definition, and the release workflow's dry run feeds the parser a real `lipo` output on every run — it has done so green twice, which is the only end-to-end evidence the accept direction is right. **Concrete next step**: add a step to the `macos` job that uploads the first 4 KB of the fused binary, commit it as a fixture, and assert the parse against it |
| P7 wave 2 | `duja-panel` `linux/mod.rs` | **A Linux laptop whose internal DRM connector publishes no EDID loses panel control entirely.** Identity comes from `/sys/class/drm/*-eDP-*/edid`, because a backlight device carries none — `intel_backlight` is a driver's name and is the same string on every machine with that chipset. If the connector's `edid` is empty (some panels never populate it; a `simpledrm` framebuffer never does), `enumerate` reports nothing at all rather than inventing an id, so the backlight is real, readable, and unreachable. Asserted by `a_backlight_with_no_internal_connector_edid_reports_nothing`, which exists to make the cost visible rather than to bless it | The alternative is a fabricated identity, and every fabricated id collides the moment a second machine runs the same code — per-display config would land on the wrong display. No honest fallback exists in sysfs: `/sys/class/backlight/<dev>` has no serial, no manufacturer, no product code. The candidates all need a real machine to evaluate: DMI product/serial from `/sys/class/dmi/id` (machine-unique, but not *display*-unique on a laptop that has had its panel replaced), or accepting the connector name as a weak id on the argument that a built-in panel cannot move ports. Neither is decidable without knowing how common the empty-EDID case actually is, which is a WSL/VM measurement |
| P7 wave 2 | `duja-panel` `backlight.rs` | **The `firmware > platform > raw` device preference rests on Duja's own argument, not on a read of the kernel.** The module documents why the order is right on its own terms — a firmware or platform device routes the change through the path the machine's own brightness keys already use, so Duja and the firmware stay in step, where a `raw` device pokes GPU registers and leaves the firmware holding a different level. The `sysfs-class-backlight` ABI file is *believed* to state the same order and that belief is recorded as unverified, because no kernel tree was read to confirm it | Cheap to settle (`Documentation/ABI/stable/sysfs-class-backlight` in any kernel checkout) and deliberately not settled by asserting it. If the ABI turns out to say something else, the argument above still stands and only the parenthetical changes; if it says the same thing, the claim gets a citation. Either way the behaviour under test does not move |
| P7 wave 2 (`#116` review) | `duja-panel` `backlight.rs` | **`ddcci` backlight devices are excluded by name, which is the pattern this project otherwise refuses.** `ddcci-backlight` registers one `/sys/class/backlight/ddcci<N>` per external monitor; they report `type = raw` like a GPU backlight and `ddcci` sorts before `intel_backlight`, so without the exclusion the panel row would drive an external monitor on any laptop with `ddcci-dkms` installed. The principled discriminator is `<dev>/device`: `intel_backlight` and `amdgpu_bl*` resolve it to the internal DRM connector, `ddcci<N>` to an i2c client | The name is a *kernel module's* documented device naming rather than a desktop's identity, which is why it is defensible where a compositor table is not — but it is still a name. Following the `device` link means asserting a sysfs layout this project cannot verify without a Linux machine, and getting it wrong would exclude the real panel instead, which is worse than the bug it fixes. Settle it on the WSL/VM box by reading what `<dev>/device` actually contains for each kind |
| P7 wave 2 (`#116` review) | `duja-panel` `linux/sysfs.rs` | **The write-channel state machine's `Unresolved` and `Logind` arms have no test coverage.** Every test constructs `Channel::Sysfs` directly, because the other two arms call `logind::Session::connect()` and there is no seam to intercept it — a GitHub runner may or may not have a system bus, so a test that exercised them would be non-deterministic in the one direction that matters. Skipping logind entirely, or failing to latch the fallback, both survive the suite | The seam is a small trait or a function pointer around one D-Bus call, and adding it means the transport becomes generic for the sake of two tests. Worth doing when `dujactl doctor` (wave 5) needs to *report* which channel is in use, since that gives the seam a second consumer and stops it being test-only scaffolding. The latching defect this row was written beside was found by review and fixed; what remains is that nothing would have caught it |
| P7 wave 3 | `duja-platform` `linux/mod.rs` | **`PlatformEvent::SessionUnlocked` has no Linux source.** The other two backends deliver it (`WTSRegisterSessionNotification`, `IORegisterForSystemPower`), so the vocabulary promises something Linux does not supply. logind's `Lock`/`Unlock` signals are the obvious candidate and are the **wrong** one: they are *requests to* a session's lock screen, not reports of its state, so a screensaver that ignores one would have Duja re-applying for a lock that never happened. The state is logind's `LockedHint` property, which needs a `PropertiesChanged` subscription on the session object | Nothing is currently broken by the gap, which is why it is a row rather than a fix: the app re-applies on `Resumed` and on `DisplaysChanged`, and an unlock that changes neither is a screen nothing disturbed. Closing it means a second D-Bus subscription with a different shape from the signal one already there (a property watch, not a signal match), and the *reason* to want it — a lock screen that resets a gamma ramp — has never been observed on Linux by this project. Worth deciding on the VM once there is a desktop session to lock |
| P7 wave 3 | `duja-platform` `desktop.rs` | **`animations_enabled()` is unconditionally `true` on Linux.** Every other query in that module reaches the real setting; this one cannot, because the XDG `org.freedesktop.appearance` namespace has **no** animations key. GNOME has `org.gnome.desktop.interface enable-animations`, KDE has its own, and reading either means a `gsettings` subprocess per query or a per-desktop table of exactly the kind ADR-0011 argues against | The behaviour is already correct rather than merely defensible: `true` is what every desktop ships with, and the cross-platform contract says a failed query answers `true`. What is missing is fidelity for the minority who turned motion off, and the only mechanism that would not violate ADR-0011's rule is a portal key that does not exist yet. Revisit if one is standardised |
| P7 wave 4 (`#121` review, `#123`) | `duja-dimmer` `linux/overlay.rs` | **Fullscreen unredirection is mitigated, not settled.** Every compositing manager unredirects a fullscreen window as a performance optimisation, and an always-on-top fullscreen window is exactly what an overlay is; unredirected means the X server draws it directly and ignores the alpha, which is the black screen `#121` exists to prevent, reached past that check. The overlay now sets `_NET_WM_BYPASS_COMPOSITOR = 2`, which is the EWMH lever for it - but that property is defined for *application* windows, and a compositor evaluates unredirection against the window it is considering. picom's `unredir-if-possible` unredirects the **whole screen** rather than one window, so a property on the overlay cannot bind that decision | There is no stronger standard mechanism to reach for, so this row is not waiting on code: it is waiting on a real session. The QA checklist gates on it explicitly (a fullscreen video and a fullscreen game, on at least one compositor with unredirection enabled), and the first community report either closes this row or turns it into a compositor-specific workaround. Recorded rather than drained because `#123` first claimed it closed, and the property being *set* is not the same as the hazard being *gone* |
| P7 wave 4 (`#123` review) | `duja-dimmer` `linux/overlay.rs` + `duja-app` `bin_support/tray.rs` | **A compositing manager that comes back does not bring the overlay back.** When the manager dies the backend tears its overlays down and latches, which is right - the connection's windows are gone and nothing re-runs the startup checks. But nothing re-runs `spawn` either: the app starts its dimmer exactly once, at launch (`tray.rs`), so a user who restarts `picom` has software dimming disabled until they restart Duja. ADR-0011 says the capability report must be re-resolved on session change, and this is the half of that which needs a *backend* restart rather than a fresh report | Needs a seam the app does not have: something that notices the session changed and asks for a new dimmer, which is the same shape as re-resolving the capability report and should share it. The QA checklist keeps the item (start a compositing manager while Duja is running) so the gap stays visible. Not urgent - the failure is a feature that stays off until restart, not a screen anybody has to recover from - and deliberately not bolted onto the overlay, where it would be a second lifecycle owner |
| P7 wave 4 (`#123` review) | `duja-dimmer` `linux/overlay.rs` | **A raise-war with another always-on-top client is damped, not solved.** X has no always-on-top: `CreateWindow` places a window above its siblings and every top-level is a sibling of an override-redirect overlay, so without re-raising the first window a user opens sits undimmed on top of the dimming. Duja re-raises on any root restack that is not one of its own windows - and so does any other client that wants to stay on top (a second OSD, an on-screen keyboard, a magnifier, a presentation tool). Two such clients trade raises for as long as both are running | Bounded rather than prevented, because no X client can prevent it: raises are damped to one per 100 ms, which turns what would be two pegged CPUs into a visible flicker, and pending restacks are coalesced so a window being dragged costs one raise rather than one per motion sample. What is left is the flicker itself, and choosing a smarter policy (back off after K rounds, or concede the top) needs a session with a real second client to watch. The QA checklist has the item |
| P7 wave 4 (`#122`), **re-scoped** `#130` | `duja-app` `bin_support/clone_group.rs` + `tray/state.rs` | **On an X11 session with no compositing manager, a mirrored pair with one software-only member goes to full brightness and its slider does nothing.** `fan_out_hardware`'s software-only rule pins every hardware-capable member of such a group to 100% on the premise that the group's one shared overlay does all the dimming, and **nothing checks that premise**. On X11 the surface token is the CRTC, so two outputs in a `--same-as` mirror really do share one and really do group; but `X11Dimmer::spawn` returns `Unsupported` when nothing owns `_NET_WM_CM_S<n>` (without a compositing manager an overlay is opaque black at every alpha), and the app then has no dimmer at all. Also reachable *mid-session*: the X11 backend latches on compositor loss and `retiring` drops the dimmer, which nothing un-latches (the "a compositing manager that comes back does not bring the overlay back" row). **The mirror is the narrow part; the software-only latch is not.** An earlier draft of this row said the latch needs the silent-no-op path (a monitor that acks a DDC write without moving) because `#116` made the Linux I2C bus never return `Backend`. The `#116` half is true and the "only" is false: `worker.rs`'s own doc lists three producers - *"a probe with no hardware range, a verified first write, or a rejected first write"* - and no classification of bus errors can bound the first of them, because that path returns `Ok(caps)` rather than any `ControlError` to classify. A monitor whose capability string is read **successfully** and simply lacks `0x10`, or one carrying the `ddc_broken` quirk (which does no wire I/O at all), latches software-only at probe time with no brightness write ever attempted. So it is a live route into this hazard, not an excluded one. (An earlier draft of *this* clause said that path "never touches the bus" - it does: `read_capabilities` writes a caps request and reads the reply, and a definitive `hardware_range == false` needs that exchange to **succeed**, since a failed read leaves the probe inconclusive) | The fix is to make the rule **ask** rather than assume - the app already has `probe_session`, whose only caller today is `dujactl doctor` (`dujactl/src/run.rs`). **`#130` briefly retired this row on the false premise that X11 always has the overlay**; it does not, and retiring it also deleted this remedy, which is why the note is spelled out here. No QA gate covers the combination either: `qa-checklist.md` tests X11-without-a-compositor and tests mirror + software-only, but the latter only on macOS |
| P7 wave 4 (`#122` review), **claim corrected and scope reduced** `#130` | `duja-app` `bin_support/clone_group.rs` | **Two earlier rows claimed Wayland mirror hazards. `#130` found both unreachable and this row is what is left of them.** The first claimed "a mirrored Wayland session gets two overlays on one screen", on the premise that a mirroring compositor exposes two `wl_output`s at one logical rectangle. Both layer-shell compositors that implement mirroring do the opposite and *withdraw* the replica's global - `KWin`'s `Workspace::updateOutputs` skips a replicated output before it becomes a `LogicalOutput`, and Hyprland's `ProtocolManager` says so in a comment (*"mirrored outputs should have their global removed, as they are not physical parts of the layout"*) - and wlroots/sway has no mirror mode at all. Its named example was worse: **Mutter implements no layer-shell**, so the scenario cannot arise there in any configuration. The second claimed `fan_out_hardware`'s software-only pin strands a mirrored pair at full brightness on Wayland; that needs a group of two, and a Wayland group is always a singleton (token = `wl_output` name, and `linux_outputs::resolve` refuses a contested pair outright, so no two displays share one), after which `fan_out_hardware` writes nothing for a lone software-only member | **What survives is hypothetical, not live:** if a compositor ever exposed two `wl_output`s covering one physical screen, per-output tokens would not collapse them and it would be dimmed twice - a grouping-key question in `duja-app`, not a dimmer one. `linux_layer::take_output` already stops the two surfaces landing on one *output*. **Not checked:** whether a Smithay-based compositor (niri, COSMIC) or Mir has a clone mode that keeps both globals; closing this needs that read or a real session. The X11 path is unaffected by *this* correction - its token is the CRTC, two outputs on one CRTC really do share a framebuffer, and that grouping is correct. That is not the same as X11 being unaffected by the pin: see the row above, where the missing piece is the overlay rather than the grouping |
| P7 wave 3 | `duja-platform` `geometry.rs` | **`cursor_anchor()` is still the debug-asserting placeholder on Linux**, so the flyout lands at the fallback rectangle rather than under the tray icon. Left for wave 4 deliberately: answering it needs an X11 connection and an extension query, which is exactly the machinery wave 4's overlay and ADR-0011's capability probe both build, and standing a second connection up here to replace it a wave later is worse than waiting | Wayland makes this more than a port: there is **no global cursor position** for a client to ask for, by design. The Linux answer is therefore two answers — an X11 pointer query, and on Wayland the `activate(x, y)` screen coordinates the SNI tray hands over (ADR-0010 records that ksni exposes them and that this is the only anchor a Wayland flyout can have). Both belong with the display-server wave |
| P7 wave 4b (`#131`) | `duja-dimmer` `linux_caps.rs` `SurfaceCaps::refuse_gamma` + `dujactl` `linux_report.rs` | **ADR-0011's step 5 has no production caller, so a `dujactl doctor` line saying "gamma dimming: available" is never downgraded by the refusal that proves it wrong.** `refuse_gamma` exists, is tested, and is called only from `linux_caps`'s own tests. `#131` built the backend that produces the signal - a `failed` event on `zwlr_gamma_control_v1` - and turns it into a `DimmerError` for the caller that asked, which is the right answer for *that* caller and reaches no report | **Not fixable where it looks like it should be fixed.** The only report on Linux comes from `probe_session`, which reads the Wayland registry and binds nothing; making it truthful by binding a gamma control would claim each output *exclusively* to answer a read-only question, and on wlroots 0.16 and earlier would evict a running `wlsunset` outright. So the downgrade needs a caller that holds a `SurfaceCaps` **across** an engage attempt, which is the app's gamma sink - the same wave the two rows below wait on. Until then `qa-checklist.md`'s row says to check the refusal rather than the doctor line |
| P7 wave 4b (`#131`) | `duja-dimmer` `linux/wlr_gamma.rs` `with_session` | **A Wayland gamma session that is open and idle never reads its socket, and a Wayland client that never reads can be disconnected by the compositor.** The X11 sibling drains with `poll_for_event` after every call and documents why; this connection has no equivalent, because there is no non-blocking `wayland-client` read that fits a synchronous design. All four entry points round-trip rather than flush, so a call that reaches the compositor drains - with one gap inside that: `release` for a name this session does not track at all returns before its round trip, deliberately, because there is nothing to hand back. (Not "an output this session never dimmed", which is how this row read for two revisions: an output that was dimmed and then given back is still tracked, and a `release` for it round-trips like any other call.) What is left is a session that engaged a dim and was then left alone while `wl_output` reconfiguration events accumulated. The consequence is worse than X11's, where the same neglect only grows a queue: here libwayland's server side answers a client it cannot write to by dropping it, which would take every live gamma control with it, silently, with the slot still reading `Open` | Narrow, and the narrowing is structural rather than luck: the events that generate that traffic - hot-plug, mode and layout changes - are the same ones that make the app re-assert its dim, which is a call, which drains. **Three write-side faces of the same root, all with the same bound.** The bound first: the outgoing buffer is per *connection*, and `EventQueue::roundtrip` cannot see its own sync answered before that sync is sent, so every round trip reaches a flush and sends whatever else is queued. Any later `set_gamma`, `enumerate_gamma_displays`, `restore_all` or `release` for a tracked name therefore rescues any of the three. What does not is a `release` for a name this session no longer tracks, or `with_session`'s transport gate, which returns before touching the connection - so the real bound is "until a call that reaches the compositor", and process lifetime only when none comes. (Two drafts of this row said the second face was bounded *only* by process lifetime, having enumerated per-*output* paths for a per-*connection* buffer.) **(a) `give_up`** flushes a `destroy` and drops the result, so a `WouldBlock` leaves the request queued and the output claimed. Usually claimed and not dimmed, because a blocked flush normally leaves the whole batch queued - but a blocked flush keeps only the remainder in both implementations (`wayland-backend`'s `BufferedSocket::flush` advances per `send_msg`; libwayland's `wl_connection_flush` does `out.tail += len` and bails on the first refusing `sendmsg`, and the app's feature graph unifies on `client_system`, so the shipped binary uses the second), so a partial write can land the 8-byte `set_gamma` and stop before the 12-byte `wl_display.sync` behind it, which leaves the output dimmed as well as claimed while the call reports failure. That split happens in the *round trip's* flush, before `give_up` runs. **(b) The registry dispatch handler**: `forget` queues a `destroy` for an output whose global was withdrawn, and no *per-output* path can carry it (`write`'s "went away" arm skips `give_up`, `release` misses `find`, `release_all` cannot see a removed entry). `#131` found it unflushed and added the flush. Not a leak by the bound above - a later call would have sent it - but a delay worth closing, because the output is held *exclusively* while it lasts. A `WouldBlock` there is what survives, and for this face the `release`-for-an-untracked-name escape is certain rather than incidental: `forget` has already removed the entry `release` looks for. **(c)** owed to the **ksni wave specifically**: `restore_all` reports clean whenever the local destroys succeeded, which is right for the two callers that exit (`duja --restore`, quit) because the socket closes behind them and the compositor drops every transform. **Two others do not exit** - the tray's "Restore screen" action, and crash-marker recovery at startup - so a blocked flush on either would report a clean restore over a still-dimmed output, and the first of those is the control a user reaches for because a screen is stuck. `bin_support::tray` is `cfg`-gated to Windows and macOS today, so `duja --restore` is Linux's only caller until the tray lands. Closing all of these properly means either a `prepare_read` + zero-timeout `poll` drain in `with_session` and a retrying flush beside it (the shape `linux/layer.rs` already uses, but ~25 lines of code no CI lane can execute) or moving this backend onto the worker-thread design the overlay uses. Neither is worth doing before something engages a ramp |
| P7 wave 4b (`#131`) | `duja-dimmer` `linux/gamma.rs` `restore_all` + `linux/mod.rs` | **A process that engaged `XRandR` ramps and then acquired a `WAYLAND_DISPLAY` reports a clean rescue over CRTCs it left dark - permanently, because an X11 ramp outlives the process.** `linux::restore_all` asks both channels precisely so a transport that moved under a running process still hands its outputs back, and that works in one direction only: `wlr_gamma::restore_all` bypasses `with_session` and reads its slot directly, so it survives a drift, while `gamma::restore_all` stops at `xrandr_refusal(session_transport())`, which answers from the *current* environment and yields `ChannelRefusal::Absent` - an empty report that `is_clean()`. So the direction with the worse consequence is the one not covered: the Wayland residual it does cover is undone when the socket closes, and this one is not | Unreachable while nothing on Linux engages a ramp, which is every build today. Not closable by dropping the gate either: without it `duja --restore` would walk Xwayland's virtual CRTCs and report a false count, and the protocol half of the Xwayland check misfires on the 22.1 branch Ubuntu 22.04 LTS and Debian bookworm ship. What closes it is the same X11-side machinery the crash-guard row above owes - a marker written before the first engage says a ramp is live regardless of what the environment says now. `linux/mod.rs`'s `restore_all` names this row; it pointed here for one commit before the row existed |
| P7 wave 4b (`#131`) | `duja-dimmer` `linux/mod.rs` `is_hdr_active` | **The Wayland gamma channel is built and nothing will engage a ramp on it, because this crate cannot tell whether the session is HDR.** `hdr_active_for` answers `None` on Wayland, so `GammaSupport` is `Unknown`, so `allows_gamma()` is `false` and a caller that respects the verdict plans an overlay instead. That has always been the answer and it was free until now - the function's own documentation argued it "costs nothing, because a Wayland session has no `XRandR` gamma channel to use it with in the first place", and `#131` removed that premise. It is the safe direction (a ramp under HDR is at best ignored and at worst a display Duja believes it has dimmed and has not) and it is not the finished one | The remedy is a probe rather than a better guess, and it is ADR-0011-shaped - a capability question, not a compositor name. The chain is four objects, not one: `wp_color_manager_v1.get_output` -> `wp_color_management_output_v1.get_image_description` -> `wp_image_description_v1.get_information` -> `wp_image_description_info_v1`, whose `tf_named` names the transfer function. A `st2084_pq` or `hlg` output is then knowably HDR. **It does not answer for every output**: the sibling event `tf_power` describes a pure power curve and names no function, so that case stays `Unknown` too, as does a compositor with no colour-management protocol at all. The XML is in the `wayland-protocols` version this workspace already builds against (0.32.13, `wp::color_management::v1`), so this needs no new package - but it **does** need a new **feature**, `staging`, which neither the workspace manifest nor `duja-dimmer`'s own declares; it resolves today only through feature unification with `smithay-client-toolkit`, which is not something to depend on. Owed by the wave that gives Linux a gamma sink to engage from - the same one the row above waits on - because until then the gate refuses a channel nothing was going to call anyway |
| P7 wave 4 (`#124`), **narrowed to X11** `#131` | `duja-dimmer` `linux/gamma.rs` + `duja-app` `bin_support/gamma.rs` | **An X11 gamma ramp outlives the process and Linux has no crash guard for it.** *(X11 only, and that is now established rather than assumed: a `zwlr_gamma_control_v1` dim lives exactly as long as the client's object, the compositor destroys every object a client holds when its socket closes, and destroying drops this client's colour transform - so the guarantee survives `SIGKILL` and a Wayland session cannot be left dark by a crash at all. (It restores the output's *default*, not some earlier client's curve: `gamma_control_destroy` emits `set_gamma` with no control attached and wlroots stores no previous table. This row said "restores the original table" through two revisions; the third caught it.) `#131` built that channel and checked the property against wlroots' `types/wlr_gamma_control_v1.c` rather than the protocol's prose alone.)* The X server holds each CRTC's table as server state and does not reset it when the writing client disconnects — which is why `xrandr --gamma` works as a one-shot command — so an **X11 session** sits with Windows, not with macOS, and needs the same machinery: a marker file written before the first engage, an RAII guard that restores identity on drop including a panic unwind, and `startup::recover_from_crash_marker` to undo a dirty exit on the next launch. None of it exists. `duja --restore` is the only recovery there, and it needs a user who can read a screen that may be at 30% | Deliberate, and the reason is that **nothing engages a ramp on Linux yet**: the engage path is the app's gamma sink, which only the tray constructs, and the tray is not built on Linux until the ksni wave. A guard added now would have no caller, and its tests would pin a lifecycle nothing drives — the dead-code shape this crate has already been burnt by (the P4 gate found `dim_mode = "gamma"` was a silent no-op for exactly that reason). Lands with the Linux sink, in the same PR, or the sink ships without a net |
| P7 wave 4 (`#124`), **scoped and widened** `#131` | `duja-dimmer` `linux/gamma.rs` + `linux/wlr_gamma.rs` | **Duja's gamma channel and any colour-temperature tool fight over one LUT, and Duja's restore wins by flattening.** *(The headline and the remedy column are **X11's**, and the row above it carries the crash guard they both wait on. `#131` established that Wayland has a collision over the same LUT but of the opposite shape, and **cannot** take the same remedy: `zwlr_gamma_control_v1` has no request to read the current gamma, so there is no baseline to snapshot. And the shape differs: this headline is last-writer-wins clobbering, which is X11's. Wayland *arbitrates* instead - the protocol grants one client exclusive access per output, so on wlroots 0.17+ Duja is simply refused while another tool holds it (no dim happens and no tint is flattened), and on 0.16 and earlier the compositor evicts that tool outright. Mutual exclusion, not clobbering, and the user-visible outcome is the opposite of this row's title. **No Wayland remedy is recorded here because none is known**: a maintainer who implements the X11 composition must not close this row. `linux/mod.rs` briefly claimed the composition was "already true on one of the two transports because the protocol does it"; the opposite is the case, and a maintainer acting on that sentence would have skipped the transport that needs the most thought.)* There is one `RandR` gamma table per CRTC and everybody shares it: `gammastep`, `redshift`, GNOME's Night Light on X11, `colord`'s calibration curve. Engaging gamma discards whatever was there, and `restore_identity` writes a linear table rather than putting it back — so a user who dims below their floor at 21:00 loses their warm tint until their tool's next update. Much more likely on Linux than on the other two platforms, where these tools are rarer | The fix is the one the macOS sink documents as deferred for the same reason: `GetCrtcGamma` **once** before the first write, compose the dim into that baseline (`baseline[i] * factor`, which for a linear dim preserves the tint *and* darkens), and write the baseline back on restore. It needs somewhere to keep a per-CRTC baseline across calls — which is the guard in the row above, so the two land together or neither does. Residual even then, and worth stating: a tool that updates the LUT mid-session gets Duja's older snapshot written back, which is still strictly better than identity. Note the two classes of victim recover differently and only one of them recovers at all: `redshift`/`gammastep`/Night Light rewrite on a timer, so they repair themselves within a minute, while a `colord`/`xcalib` **calibration** curve is loaded once at login and stays flattened until the next one |
| P7 wave 4 (`#124` review) | `duja-dimmer` `linux/gamma.rs` + `linux/outputs.rs` | **Only the default X screen is ever walked, so a Zaphod-mode server gets a clean report for a rescue that missed half of it.** `open()` takes `setup().roots.get(screen)` for the screen `DISPLAY` names, and `GetScreenResourcesCurrent` is per screen — so on a dual-GPU server configured without Xinerama (`:0.0` / `:0.1`), `duja --restore` walks one screen, prints `restored identity gamma on N CRTC(s)`, exits 0, and leaves the other screen's ramp exactly as it was. Same shape as the two defects the reviews of this PR already caught: a success report over a screen that is still dimmed | Not a one-line fix, and not gamma-specific: `linux::outputs` has the identical single-root limit, so closing it here alone would make the enumeration and the rescue disagree about what a session contains. The two want changing together, and the change is a loop over `setup().roots` with a per-screen root threaded through both walks. Deferred because Zaphod mode is a deprecated configuration nobody in this project can test on, and because the *code* is currently self-consistent — it is the wording that was overstating, and that is fixed. Revisit if any P7 tester turns out to run one |
| P7 wave 4 (`#124` review) | `duja-ui` `ui/settings.slint` + `duja-app` `bin_support/settings.rs` | **The gamma hazard caption names macOS, and `gamma_is_advisory()` is now true on Linux too.** `#103` added the caption for one platform and worded it for that platform: *"macOS can accept a ramp and not apply it — on some recent Macs regardless of settings, and on others while 'Automatically adjust brightness' is on"*. `#124` made the predicate true on X11 for a **different and sharper** mechanism: `ProcRRSetCrtcGamma` discards `RRCrtcGammaSet`'s return, which is the driver hook's own result, and answers `Success` regardless — so there is no setting to blame and no hardware subset, it is every X11 write. A Linux user would be shown macOS copy about a Mac feature they do not have | Not reachable today, which is why it is a row and not a fix: `platform_gamma_limits()` has one caller, `tray/state.rs`, and `mod tray` is `cfg(any(windows, target_os = "macos"))` — so no Linux user can see the caption until the ksni wave un-gates it. That wave owes this: the caption needs to select its text on the platform (or state the shared fact — an accepted gamma write is not proof of a dimmed screen — and drop the per-OS cause). Recorded because the failure mode is silent: the code is correct, the string is wrong, and nothing tests a translated caption's *content* |

Drained at P2 gate (2026-07-08): MSI MP273QP quirk rows encoded in `quirks/quirks.toml`; contract suite hardened against `max`-lying backends (ADR-0002); unstamped-config migration semantics fixed (ADR-0007).

Drained at P3 gate (2026-07-10): `ResolvedQuirks` fields are now consumed by the
dxva2 backend (pacing, caps retry, verify-writes, `max_brightness`,
`input_source_allowed`, `no_input_switch`, `caps_unreliable`, `ddc_broken`).

Drained at P4 (2026-07-10): the ubuntu Slint system-deps step (fontconfig +
xkbcommon + xcb + wayland) is wired into the `clippy`, `test`, and `docs` CI
jobs now that `duja-ui` brings Slint into the workspace.

Drained at P5 gate (2026-07-10): `dujactl`'s `duja-ipc` dependency is now a real
consumer (IPC client); full DDC capability probing landed for input sources
(`Capabilities.allowed_inputs` = caps ∩ quirks). Gate findings fixed: the IPC
read deadline is armed once per exchange (a dribbling peer can no longer renew
it per syscall and pin a handler thread) and the first pipe instance is closed
on thread-spawn failure.

Drained by the P0 live-QA fix (2026-07-11): the `tray.rs` `APP.borrow_mut()`
-across-`update_from_vm` latent double-borrow is gone — all `AppState` access now
goes through the re-entrancy-safe `ReentrantCell` dispatcher (`with_app`), and
the *actual* live-QA crash (the settings shell callbacks held a `SettingsVm`
`borrow_mut` across the app's re-render `borrow`) is fixed by releasing the
borrow before the handler runs.

Drained by the settings-widget rebuild (2026-07-15): the settings window's
std-widgets (`Slider`, `ComboBox`, `Button`) are replaced by custom Palette-driven
components — `TrackSlider`, `Dropdown`, `TextButton` in `flyout.slint`. This closes
three coupled issues at once: (1) they now size identically across the app; (2)
their labels draw from our `Palette.text`, curing the light-theme invisibility of
the std widgets (which followed the OS colour-scheme, not our app theme); and (3)
the per-monitor sliders now follow the chosen accent like `BrightnessSlider`. Only
`ScrollView` remains from std-widgets (a container, no text or accent surface).

Drained at the v0.1.1 deep-review fix wave (2026-07-17, PRs #46–#55): the
SHA256SUMS CRLF bug (now LF, `#47`); the engine shutdown-hang / detached-worker
double-write / OpenFailed-loses-control / watchdog-recovery-relearn defects (the
generation + `retired` + bounded-shutdown backbone, ADR-0017, `#48`); the overlay
HWND leak, mandatory-capture-exclusion, crash-marker-cleared-on-failed-restore, and
UI-freeze-on-wedged-overlay defects (`#49`, `#53`); the `dujactl set all`-over-IPC
break (`#46`); the hot-plug-during-drag wrong-monitor retarget (`#52`); the
zero-max panel-dark bug (`#54`); the serial-less EDID identity split (`#50`); the
frozen-at-launch HDR verdict (`#53`); the update-check future-timestamp wedge,
motion-default, logging-fallback, and flyout-off-screen-on-resize bugs (`#51`); and
the supply-chain release-pipeline injection / un-gated-publish / non-hermetic-build
gaps + the `dujactl` server-SID verification (`#47`, `#55`). Every fix landed
test-first (red-first regression proven) and was reviewed by a separate adversarial
agent. The verify-by-readback tolerance and `brightness_max` sanity are now pinned
by tests (`#54`).

Drained by the tray module split (2026-07-26): the 2,821-line `duja-app`
`bin_support/tray.rs` god-module is now `tray.rs` (the `run()` assembly,
`ReentrantCell`/`with_app`, and the small OS/glue helpers) plus `tray/policy.rs`
(pure decision rules), `tray/state.rs` (`AppState` + the bulk of its `impl`),
`tray/update_flow.rs` (the update-check `AppState` methods), `tray/wiring.rs`
(tray/menu build + every foreign-event registration), and `tray/hotkey_os.rs`
(the `global_hotkey` registrar and accelerator conversion), alongside the
existing `tray/geometry.rs` and `tray/icon.rs`. A pure move-and-rewire: no
behaviour change, no signature change beyond `pub(super)`, and all 22 tests moved
with the code they cover. `run()` keeps its `#[allow(clippy::too_many_lines)]`
with a rewritten rationale — the split deliberately did not shorten it (see the
comment above `run`).

Drained by the macOS tray-anchor backend (2026-07-30): the `#87` "no ADR records
the tray-geometry coordinate contract" row is closed by
[ADR-0021](adr/0021-tray-anchor-coordinate-contract.md), which it was waiting on
the macOS backend to make whole — `AnchorUnit` names the unit and the two derived
factors (`logical_to_anchor`, `anchor_to_physical`) split the single `×scale`
conversion per platform, with their product pinned to the sanitised `scale` on
both variants. The mixed-DPI winit residual it surfaced is a *new*, narrower row
above, not a carry-over of the old one.

Half-drained by the test-infra pass (2026-07-26): the v0.1.1 deep-review row is
**split**, not closed.

The **headless E2E smoke** half is done: `crates/duja-app/tests/e2e_smoke.rs`
stands up a fake backend (injected enumerator + controllers, so no displays are
needed) behind the real engine, the real `IpcBridge`/`handle_request` — hoisted
out of the binary into the `duja_app::ipc` library module so an integration test
can reach them — and the real OS transport, driven with
`list`/`set`/`get`/`show-flyout` and asserted on the resulting hardware writes
and snapshots. It runs under the required `test` job's
`cargo nextest run --workspace --all-features` on all three OS lanes (both IPC
transports), so it needs no CI job of its own; a dedicated `e2e-smoke` job was
added and then removed as a strict, unrequired subset of that.

The **throttle-final-value** half is only partly done, and the residue is the
row restored above. The slider→engine forwarding is now an injectable sink
(`bin_support/level_forward.rs`: a `LevelSink` trait + a `LevelForwarder` that
owns it, the same shape as `GammaSink`/`IpcBridge`/`HotkeyRegistrar`) — but
pinning the contract *there* proved worthless: `LevelForwarder::forward` is the
last link in the path and a throttle is only ever re-added upstream of it, so
re-inserting the historical throttle at its actual site
(`AppState::set_user_level`) left every test green. The real pin now lives in
`duja-ui`, where `shell.rs`'s `slider_drag_burst_emits_the_released_value_last`
bursts a drag through the real Slint binding and asserts the released value is
the last `UiCommand::SetLevel` emitted; it was proven red twice, against a
leading-edge throttle in the shell's `slider-changed` handler and against one in
`FlyoutVm::slider_changed` (both delivered `[50]` of a 50→70 burst). The app
layer in between stays unpinned for the reason recorded in the row.

Drained at P7 wave 5 (2026-08-03): `dujactl doctor` gained its Linux section, so
the two rows that were waiting for "somewhere a capability report can be printed"
are closed. The i2c row is fully drained — every connector now reports whether
its DDC/CI channel is reachable and, where one exists, the remedy. The
sleep-events row is **partly** drained: `doctor` reports what the *display* side
found, and whether `PrepareForSleep` is subscribed is still not surfaced, because
the pump does not hand that fact out. Re-file it if it bites.

Drained at P7 wave 4 (2026-08-03): a Linux display finally has a rectangle, so
the two rows that were waiting for the display server are closed. Connectors are
joined to X11 `RandR` outputs or Wayland `wl_output`s **by name and EDID
together, then EDID alone, then a bare name only where the server published no
EDID to check it with**. The EDID is the fallback the name row was opened to
demand: the NVIDIA proprietary driver's own indexing and the legacy
`xf86-video-intel` DDX's hyphen-less names both defeat string equality, and
neither defeats the EDID.
A name match that a present EDID contradicts is **not** taken: the NVIDIA
namespaces overlap and are offset by one, so a name equality there names the
neighbouring monitor, and trusting it would place two of three displays on the
wrong screen. Ambiguity refuses in both directions — this connector matches no
other output, and no other connector matches this output — so two identical
monitors stay unplaced whether the ambiguity is on the output side or the
connector side. Neither driver is verified by this project; what changed is that
a name mismatch now costs a display nothing, and a name *collision* costs it
nothing worse than a refusal.

Drained by the X11 overlay (2026-08-03): the compositor-stops row `#121` left
open is closed. A second thread holds `XFixesSelectSelectionInput` on
`_NET_WM_CM_S<n>`, tears every overlay down the moment the owner goes to `None`,
and **latches** so the next apply cannot map fresh windows onto a session that
can no longer blend them - which is the `refuse_gamma` analogue that row asked
for, both halves of it. Losing the dimming when `picom` dies is a visible,
recoverable degradation; keeping it is a screen the user cannot see to fix. The
unredirection row is **not** drained: setting `_NET_WM_BYPASS_COMPOSITOR = 2` is
the only standard lever there is, and it is a mitigation rather than a
settlement - see its row, rewritten rather than removed.
