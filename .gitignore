/target
**/*.rs.bk
*.pdb

# cargo-fuzz
fuzz/target
fuzz/artifacts
fuzz/coverage
# Corpus entries libFuzzer generates during a run are not committed; the small
# hand-picked seed inputs are force-added (see fuzz/README.md).
fuzz/corpus/

# coverage output
lcov.info
coverage/

# editor noise
.vscode/
.idea/
*.swp

# local-only environment
.env

# Claude agent worktrees (local session artifacts)
.claude/worktrees/
